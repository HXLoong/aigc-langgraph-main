# plan0909 · golden 测试脚本改造方案（2026-09-15）

- 分支：`feature-kwj`
- 数据源：`tests/fixtures/categories/*.jsonl` 唯一语料，6 文件 / 389 条 / 两种方言
- 运行形态：`python -m harness run`，先启动 uvicorn，经 HTTP 驱动，默认真后端，不启 MySQL checkpoint
- 边界：不修改 fixture 数据内容；除第 1 笔 outputs 契约字段外，不修改 `app/` 业务代码与提示词

## 1. 改造方案

### P0 · 断言正确性与可观测性

| 编号 | 改造内容 | 位置 | 具体做法 |
|---|---|---|---|
| R1 | `expected.winners` 断言恒 FAIL：outputs 无 `winners` / `winner`，只有 `tickers`，按 key 盲比必产生 diff，case-021 / 025–029 必误报 | `harness/differ.py:73-76` | 实现定稿为 `check_structured_assertions` 单点特判：`winners` 从 `expected` 弹出单独比较 `set(expected["winners"]) == {t["wind_code"] for t in outputs.get("tickers", [])}`（集合相等、去重忽略顺序），其余键仍按 `diff_fields` 直比，`expected` 无 `winners` 时跳过特判；actual 读 `tickers[].wind_code`，outputs 为 `model_dump()` snake_case。不改 `normalize_case`，不引入 alias 框架。候选超集容忍度按附录 #1 复核 |
| R2 | outputs 缺 `api_code` / `api_result` / `trace`，HTTP 形态下状态码判定不可用，核销凭据三字段恒空 | `app/api/routes.py` `_state_to_outputs` | ① key 循环追加 `api_code` / `api_result` ② `trace` 压缩为 `node[decision] → ...` 单行字符串后输出 ③ 测试落点 `tests/test_api.py`，先写失败断言再实现 ④ 独立成笔，见 §4 第 1 笔 |

### P1 · `run` 前置门禁

- `harness/cli.py:120-123` `_run` 入口前置 `GET /health`，非 200 退出码 2
- 前置 `GET /ready`，除 `mysql` 外任一 `fail` 退出码 2；`--checkpoint none` 时容忍 mysql fail
- `--backend` / `--checkpoint` 启动时打印声明与「写类真跑」警示
- `--user-id` / `--room-id` 未显式传入时从 repo `.env` 读取 `EVAL_USER_ID` / `EVAL_ROOM_ID`；仍为空则退出码 2

### P2 · 轮次语义收敛

- 抽取纯函数：quote 选择、不可用判定、早停谓词；入参只用基本类型（`prev_replies: list[str]`、`quote_previous: bool | None`、`index: int`），供 `harness/multi_turn.py:46-54,140-153` 与 `scripts/langfuse_eval.py:88,107-112,123-245` 共用
- `quote_previous=None` 语义定稿：第 1 轮 `index == 0` 时 quote 必为空；第 2 轮起取第 1 轮回复，回溯跳过不可用回复。`scripts/langfuse_eval.py` 现取「上一轮」，随本次重构对齐
- `scripts/langfuse_eval.py` 保留进程内循环，删除本地重复实现

### P3 · runner / reporter 清理

- 删除 `harness/runner.py`
- 删除 `harness/reporter.py`；`build_overview` 移入 `harness/golden.py`，由 `scripts/upload_golden_to_langfuse.py:45` 与 `scripts/langfuse_eval.py:454` 共用
- `harness/cli.py:73` `_render_markdown` 保持独立
- 改写 4 个旧测试：`test_reporter_*` 3 个删除；`test_token_tracker.py` 剥离 reporter 依赖，仅保留 tracker 本体测试

### P4 · 入口与删除收尾

- `scripts/probe_real_backend_e2e.py` 保留原状，不并入 `harness doctor`；CLI 不设 `sync-golden` / `eval` 子命令，相关操作直接用 `scripts/` 命令
- 删除清单，执行前 `git grep` 核查引用，含 CI / shell / 文档：
  - `scripts/`：`eval_golden.py` / `demo_closed_loop.py` / `merge_golden.py` / `convert_testcase_to_jsonl.py` / `upload_option_dataset.py` / `upload_dataset_to_langfuse.py` / `langfuse_eval_clean.py` / `shadow_compare.py`
  - `tests/`：`test_eval_golden.py` / `test_shadow_compare.py`
  - `scripts/probe_ticker_d2_4.py` 的 `--fixture` 模式；`tests/test_intent_route.py::TestGoldenRuleCoverage`

### P5 · 测试与规范收尾

- 新建 `tests/test_harness_multi_turn.py`：quote 回溯 / 早停 / at_bot 透传等 8 用例
- CI 增加 `mypy harness/`
- `harness/multi_turn.py:85` `api_code` 比较点写 `isinstance(code, int) and code != 0`
- `harness/golden.py:68` `_turn_from_object` 的 `at_bot` 改为透传原值，由 `TurnSpec.at_bot` 校验
- `scripts/upload_golden_to_langfuse.py:69` `build_expected` 回退拼接 `response_contains`，docstring 注明为评估层近似期望

## 2. 验收

| 编号 | 验收标准 |
|---|---|
| B4 | `api_code` 非 0 记 `business_reject` 分类；case 通过与否以断言为准；负例期望通过 `response_contains` 表达 |
| B5 | reply 含 `DRY-RUN-` 即 FAIL；`default_reply` 与 GOATS 0 命中文案由 fixture `response_not_contains` 承担 |
| B6 | 报告逐轮含 `tickers[].from_goats`；模糊识别类用例以该值作诊断，不构成 FAIL 条件 |
| B7 | `expected.output` 不参与确定性 diff |
| C8 | `/health` 非 200 或 `/ready` 除 mysql 外任一 `fail` 时退出码 2，不执行用例 |
| C9 | 报告落 `.harness-runs/probe-{ts}/`，含 `summary.json` / `report.md` / `failures/` |
| C10 | 核销凭据逐轮含 `api_code` / `api_result` / `error` / `trace` / `place_params` / `quote_passed` |
| C11 | banner 打印服务端 URL、`--backend` / `--checkpoint` 声明值与写类警示 |
| D12 | `scripts/check_fixture_consistency.py` 退出码 0 |
| D13 | `langfuse_eval.py` 富集 JSON 保持 `{expected, score, judge_comment, turns[turn/raw/reply/product_type/intent/tickers/place_params/api_result/api_code/error/trace/quote_passed]}` |
| D14 | 全量 pytest 无新增失败 |

## 3. 风险与对策

| 风险 | 对策 |
|---|---|
| 389 条真跑含写类，会在后端产生真实订单/撤单，不可逆 | 强制 `EVAL_USER_ID` / `EVAL_ROOM_ID`；banner 打印目标 URL 与写类警示；`--case` / `--category` / `--limit` 子集；`--stop-on-fail`；`--backend dry-run` 逃生门，需以对应环境重启 uvicorn；`--backend` / `--checkpoint` 仅打印声明，防护依赖本节其余对策 |
| 不启 MySQL checkpoint 时多轮降级 | 上下文仅由 `quote_content` 传递，dialect A 13 条 option 用例受影响；需要生产同款语义时以 `USE_MYSQL_CHECKPOINTER=true` 复跑 option 子集，见附录 #2 |
| 真后端轮间 dedup | `--turn-interval` 真后端设 1.0s；dedup 回复触发该轮断言失败或 `business_reject` 早停 |
| 运行中隧道 / GOATS 掉线 | envelope `status=failed` 触发 `node_error` 早停；GOATS 降级文案由 fixture `response_not_contains` 覆盖；启动时不可达由 P1 门禁拦截 |

## 4. 提交组织与里程碑

| # | commit message | 对象 | 验证 | 批次 |
|---|---|---|---|---|
| 1 | `feat(api): outputs 补 api_code / api_result / trace 核销字段` | `app/api/routes.py` · `tests/test_api.py` | `pytest tests/test_api.py -q` | 打通可信链路 |
| 2 | `fix(harness): winners→tickers 断言映射` | `harness/differ.py` · `tests/test_harness.py` | `pytest tests/test_harness.py -q` | 打通可信链路 |
| 3 | `feat(harness): run 前置门禁 + EVAL 变量加载` | `harness/cli.py` · `tests/test_harness.py` | `pytest tests/test_harness.py -q` | 打通可信链路 |
| 4 | `refactor(eval): 轮次语义纯函数收敛 + 多轮 8 用例` | `harness/multi_turn.py` · `scripts/langfuse_eval.py` · `tests/test_harness_multi_turn.py` | `pytest tests/test_harness.py tests/test_harness_multi_turn.py tests/test_langfuse_eval.py tests/test_langfuse_eval_pipeline.py -q` | 收口 |
| 5 | `chore(harness): runner/reporter 清理 + 规范收尾` | `harness/golden.py` · 删除 `runner.py` / `reporter.py` · `scripts/upload_golden_to_langfuse.py` · CI mypy | `pytest tests/test_harness.py tests/test_token_tracker.py -q` · `mypy harness/` | 收口 |
| 6 | `chore(test): 遗留脚本与测试删除` | `scripts/*` · `tests/*` | `git grep` 旧引用清零 · `pytest tests/ -q --tb=line` | 收口 |

> 测试文件在 §5 重组前统一位于 `tests/` 根目录。

里程碑 A · 首轮可信回归，第 1–3 笔合入后执行：

```powershell
.venv\Scripts\python.exe -m harness doctor --base-url http://127.0.0.1:8000 --checkpoint none
# 只读 smoke：询价 4 条，不产生写操作，避免与全量跑重复下单触发 dedup
.venv\Scripts\python.exe -m harness run --base-url http://127.0.0.1:8000 --backend real `
  --data tests/fixtures/categories --category option/inquiry --turn-interval 1.0 --stop-on-fail
# 全量
.venv\Scripts\python.exe -m harness run --base-url http://127.0.0.1:8000 --backend real `
  --data tests/fixtures/categories --turn-interval 1.0
```

里程碑 B · 清理后多轮回归，第 4–6 笔合入后执行：

```powershell
# case-025 为 4 轮用例，覆盖 quote 链与写类路径；执行会产生真实订单
.venv\Scripts\python.exe -m harness run --base-url http://127.0.0.1:8000 --backend real `
  --data tests/fixtures/categories --case case-025 --stop-on-fail
```

---

## 5. 目录归类整理

独立 PR 执行；物理重组在 §1 全部落地后开始。

### 5.0 两层策略

| 层 | 内容 | 时机 |
|---|---|---|
| 逻辑归类 | 更新 `scripts/CLAUDE.md` 分类表；`tests/CLAUDE.md` 补分类页 | 随 P4 一并提交 |
| 物理重组 | 文件移入子目录 + 引用全量更新，见 §5.3 | 独立 PR |

### 5.1 `scripts/` 目标结构

仅 eval / deploy / prompts 建子目录，其余平铺根级，与 `tests/scripts/test_X.py` 逐文件对位。

```
scripts/
├── eval/                    # 评估与数据集：langfuse_eval.py · upload_golden_to_langfuse.py
│                            #   · run_option_eval.sh · run_real_llm_harness.sh
├── deploy/                  # deploy-customer.sh
├── prompts/                 # 提示词资产工具：export_dify_prompts.py · promote_langfuse_prompt.py
├── ai_test_langgraph/       # 回归参考实现，整块保留
│
│  # —— 根级平铺（scripts/X.py ↔ tests/scripts/test_X.py）——
├── check_fixture_consistency.py · check_adr_refs.py · check_alert_threshold_consistency.py  # CI lint
├── probe_real_backend.py · probe_real_backend_e2e.py · probe_{swap,option,close,ticker}_write_e2e.py
├── probe_ticker_d2_4.py · probe_fast_query.py · diagnose_close_timeout.py  # 真后端探针（VPN）
└── canary_status.py · metrics_snapshot.py · run_alerts.py · llm_cost_report.py
    · cleanup_checkpoints.py · rollback_canary.sh · drill_smoke.sh          # 灰度 / 运维 / 成本
```

> 真后端 golden 回归主入口为 `python -m harness run --base-url <uvicorn> --backend real`，harness 经 HTTP 访问已启动的 uvicorn；`scripts/probe_*.py` 保留为连通性探针与写类专项探针。

### 5.2 `tests/` 目标结构

```
tests/
├── harness/                 # 评测台 + fixture 消费
│   ├── test_harness.py / test_harness_multi_turn.py
│   ├── test_fixture_consistency.py
│   ├── test_case_generator.py / test_llm_paraphrase.py
│   └── test_token_tracker.py
├── prompts/                 # 提示词治理
│   ├── test_prompt_governance.py / test_prompt_loader.py / test_prompt_versioning.py
│   └── test_promote_langfuse_prompt.py / test_langfuse_prompt_gate.py
├── scripts/                 # scripts/ 测试镜像
│   ├── test_canary_status.py / test_metrics_snapshot.py / test_rollback_canary.py
│   ├── test_drill_smoke.py / test_cleanup_checkpoints.py
│   └── test_check_adr_refs.py / test_alert_threshold_lint.py / test_probe_real_backend_e2e.py
├── nodes/ graph/ subgraphs/ integration/ observability/ tools/ api/   # 已有镜像结构，不动
├── fixtures/                # 不动
├── conftest.py              # 保持根，子目录自动继承
└── test_smoke.py            # 保留顶层（第一入口）
```

注意事项：

- `test_api.py` 不迁入 `tests/api/`；该目录被 `addopts = "--ignore=tests/api"` 排除。
- `diagnose_close_timeout.py` 归 `scripts/`；`test_case.md`、`业务流程.png` 归 `docs/` 或删除。
- `test_intent_route.py` 归 `tests/nodes/`。
- `test_langfuse_eval.py` / `test_langfuse_eval_pipeline.py` 保留在 `tests/` 顶层。

### 5.3 迁移清单

| 引用面 | 实测位置 | 动作 |
|---|---|---|
| 测试 import 脚本模块（16 处） | `tests/observability/test_cost_report.py`、`tests/observability/test_exit_gate_fixes.py`、`tests/test_alert_threshold_lint.py`、`tests/test_canary_status.py`、`tests/test_check_adr_refs.py`、`tests/test_cleanup_checkpoints.py`、`tests/test_fixture_consistency.py`、`tests/test_langfuse_eval.py`、`tests/test_langfuse_eval_pipeline.py`、`tests/test_metrics_snapshot.py`、`tests/test_probe_real_backend_e2e.py`、`tests/test_promote_langfuse_prompt.py`、`tests/test_prompt_governance.py`、`tests/test_rollback_canary.py`、`tests/test_shadow_compare.py`、`tests/test_eval_golden.py` | 改 import / 路径；后两者随 P4 删除 |
| 脚本内仓库根推断（≥5 处） | `check_adr_refs.py:33`、`check_alert_threshold_consistency.py:32`、`check_fixture_consistency.py:21`、`probe_real_backend_e2e.py:37-39`、`promote_langfuse_prompt.py:31-33` | 维持 `parents[1]`；迁移前全量排查 `__file__` 用法 |
| shell 硬编码路径（4 文件） | `drill_smoke.sh`（117-121 / 143-149 / 210 / 235 / 260）、`deploy-customer.sh`（212 / 529）、`run_option_eval.sh`（6）、`rollback_canary.sh`（430） | 同步改路径 |
| CI | `.github/workflows/ci.yml:41 / 46 / 51` | 改路径 |
| 文档 / 规则 / 技能（≥10 文件） | `CLAUDE.md`、`.claude/rules/testing.md`、`.claude/rules/git-workflow.md`、`.claude/skills/{run-eval,shadow-test,iterate-option}/SKILL.md`、`scripts/CLAUDE.md`、`tests/CLAUDE.md`、`docs/m3-m4-roadmap.md`、`docs/on-call-runbook.md`、`harness/README.md` | 全文替换路径 |
| 测试内相对 fixture 路径（3-4 处） | `tests/test_intent_route.py` 等 `Path(__file__).parent / "fixtures"` 用法 | 改层级或改仓库根相对 |

### 5.4 与 §1 的交叉项

| 文件 | 处理 |
|---|---|
| `tests/test_shadow_compare.py` | 随 P4 删除 |
| `tests/test_langfuse_eval.py` | P2 改写：改 import 共享纯函数 |
| `tests/test_langfuse_eval_pipeline.py` | P2 改写：保留进程内循环，改 import 共享纯函数 |

### 5.5 命名约定

- `scripts/`：动词前缀 `check_*` / `probe_*` / `upload_*` / `run_*`；仅 eval / deploy / prompts 建子目录，其余平铺根级。
- `tests/`：文件名 = 被测模块名；`tests/scripts/` 与 `tests/harness/` 按来源目录镜像；新增脚本必须带同名测试。
- 包结构：补齐 `scripts/__init__.py` 与 `scripts/{eval,deploy,prompts}/__init__.py`；import 为 `scripts.<module>` 或 `scripts.<group>.<module>`；直接执行 `python scripts/[<group>/]<name>.py` 不受影响。

### 5.6 执行顺序与验收

1. 前置：工作区干净、§1 全部合入。
2. 移 `scripts/`：建目录 → `git mv` → 改测试 import → 改 shell / CI；仅 eval / deploy / prompts 建目录，其余平铺，`parents` 深度维持 `parents[1]`。
3. 移 `tests/`：`git mv` → 改相对 fixture 路径。
4. 文档同步：§5.3 清单 + `scripts/CLAUDE.md` / `tests/CLAUDE.md` 分类表重写。

```powershell
# 旧路径引用清零：按 §5.3 清单逐项执行，无输出即通过
git grep -n -E "scripts/(check_fixture_consistency|check_adr_refs|canary_status|metrics_snapshot)"
git grep -n -E "^from scripts\.|^from scripts import|sys\.path.*scripts"
# 全量回归 + 守门
.venv\Scripts\python.exe -m pytest tests/ -q --tb=line
.venv\Scripts\python.exe scripts/check_fixture_consistency.py --verbose
```

完成判据：旧路径引用为零、全量测试通过、CI 绿、两份 `CLAUDE.md` 分类表与新结构一致。

### 5.7 风险

| 风险 | 对策 |
|---|---|
| 未提交改动与移动冲突 | 迁移前确认工作区干净、开单独分支 |
| 引用漏改 | 以 `git grep` 清零为准入条件 |
| 新增 `scripts/__init__.py` 与 3 个子目录改变导入语义 | 一次性全改；`pyproject` 已配 `pythonpath=["."]`，包路径兼容 |

---

## 6. 附录 · 运行后确认

| 编号 | 事项 | 动作 |
|---|---|---|
| 1 | 断言口径复核：`response_contains_any` 任一命中 vs 全命中；`winners` 候选超集是否容忍 | 里程碑 A 后以全量结果与数据提供方确认；口径变化另开改动 |
| 2 | 多轮语义确认 | sign-off 前以 `USE_MYSQL_CHECKPOINTER=true` 复跑 option 子集，量化与默认配置差异 |
