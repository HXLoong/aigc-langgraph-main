# plan0909 · golden 测试脚本改造方案

- 日期：2026-09-09（2026-09-10 审校重排）
- 分支：`feature-kwj`
- 范围：读写 `tests/fixtures/**` 的脚本；`scripts/ai_test_langgraph/**` 仅作参考实现，不在改造范围
- 约束：不改 `app/` 业务代码与提示词；不改 fixture 数据内容
- 数据源结论：**`tests/fixtures/categories/` 为唯一数据源（6 文件 / 389 条 / 两种方言）**；`old_typing/` 与 `unified_golden.jsonl` 为归档，任何脚本不得读取

---

## 0. 摘要

### 0.1 已确认问题（实测）

| 编号 | 问题 | 证据 |
|---|---|---|
| P0-1 | 现 loader 与现役数据不兼容：方言 B（swap 任务队列）抛 `ValidationError: id Field required`；方言 A（option）静默丢弃全部轮次（`raw_content == ''`） | `harness/golden.py:38-52`；实测 `load_golden('tests/fixtures/categories/swap_prod_data.jsonl')` 抛错、`golden_option_open_case.jsonl` 载入后 `raw_content=''` |
| P0-2 | `harness run` 默认路径只命中归档 `unified_golden.jsonl`，链路 A 实际跑在旧数据上 | `harness/cli.py:32-37` |
| P0-3 | `expected.output` 为自然语言期望，`AgentState` 无对应字段，`diff_fields` 对其必然产生 diff → 链路 A 恒 FAIL | `harness/differ.py:70-73` |
| P0-4 | 多轮从未执行：`harness/golden.py:70-74` 只保留 `conversation[0]`，`harness/runner.py` 单次 `ainvoke` | 代码 |
| P1-1 | `scripts/check_fixture_consistency.py` 恒 `exit 2`：依赖 6 个已失效路径（5 个已移入 `old_typing/`），锚点校验对象不存在 | `:24-32`、`:84-100` |
| P2-1 | 文档与实现不符 ≥8 处（多处推荐已失效的 `eval_golden.py`） | §3.7 文档同步 |

### 0.2 处置总览

| 处置 | 对象 |
|---|---|
| 重写 | `harness/golden.py`（§3.1）、`scripts/check_fixture_consistency.py`（§3.6） |
| 新增 | `harness/multi_turn.py`（§3.2）、`harness/backend.py`（§3.4）、`tests/test_harness_multi_turn.py`（§3.2） |
| 改造 | `harness/runner.py`、`harness/differ.py`（§3.3）；`harness/cli.py`、`harness/case_generator/*`（§3.4）；`scripts/langfuse_eval.py`、`scripts/upload_golden_to_langfuse.py`（§3.5）；`tests/test_harness.py`、`tests/test_fixture_consistency.py` |
| 删除 | 8 个脚本、2 个测试文件、1 个测试类、1 个脚本模式（§3.7） |

### 0.3 目标

1. **可跑**：保留链路按 `categories/` 轮次语义完整执行多轮，行为一致。
2. **可控**：后端三档（`mock` / `dry-run` / `real`）命令行切换 + 启动自检。
3. **可维护**：单一归一化层 + 单一多轮执行器；删除失效脚本与重复实现。

非目标：不新增业务能力；不改 `app/` 与 `app/prompts/`；不重新标注 fixture 内容。

---

## 1. 现状

### 1.1 数据源：`tests/fixtures/categories/`（6 文件 / 389 条）

| 文件 | 条数 | 方言 | 说明 |
|---|---:|---|---|
| `golden_option_open_case.jsonl` | 5 | A · 结构化 | 开仓链路，含多轮 `sub_scenes` |
| `golden_option_inquiry_case.jsonl` | 4 | A · 结构化 | 询价链路，单轮（`sub_scenes` 为空） |
| `golden_option_close_case.jsonl` | 4 | A · 结构化 | 平仓链路，含多轮 |
| `swap_prod_data.jsonl` | 123 | B · 任务队列 | 生产互换数据 |
| `swap_prod_acceptance_data.jsonl` | 163 | B · 任务队列 | 验收互换数据 |
| `swap_test_fuzzy_target_recog_data.jsonl` | 90 | B · 任务队列 | 标的模糊识别，含 `response_contains_any` |

归档资产（不读取）：`tests/fixtures/old_typing/`（10 个文件）、`tests/fixtures/unified_golden.jsonl`（921 条）。

### 1.2 方言字段规范

**方言 A · 结构化用例（13 条）**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` / `category` / `name` / `caseNo` | str | 是 | `category` 取值：`option/inquiry`、`option/place_from_quote`、`option/cancel`、`option_close/place` |
| `scene` / `send_text` | str | 是 | 第 1 轮场景名与输入 |
| `at_bot` | bool | 是 | 第 1 轮是否 @机器人 |
| `expected` | obj | 否 | `{product_type, intent, winners?, output?}`；4 条询价用例中仅 1 条提供 |
| `response_contains` / `response_not_contains` | str[] | 是 | 全含 / 全不含断言 |
| `sub_scenes` | obj[] | 是 | 第 2 轮起，可为空数组 |

`sub_scenes[]` 字段：`scene`、`send_text`（必填）、`at_bot`（缺省 `False`）、`quote_previous`、`expected`、`response_contains`、`response_not_contains`。

**方言 B · 任务队列用例（376 条）**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` / `caseNo` | str | 是 | 无 `id` / `category` |
| `scene` | str | 否 | 仅模糊识别文件提供 |
| `send_text` | str | 是 | 单轮输入 |
| `response_contains` | str | 是 | 多行字符串，逐行拆为子串断言 |
| `response_contains_any` | str | 否 | 多行字符串（47/90 条模糊识别用例使用） |
| `response_not_contains` | str | 否 | 多行字符串 |

方言 B 无 `expected` / `at_bot` / `sub_scenes`：仅做文本断言。

### 1.3 轮次与引用语义（对齐参考实现 `scripts/ai_test_langgraph/langgraph_direct_regression.py`）

| 项 | 规则 |
|---|---|
| 轮次来源 | 第 1 轮 = 顶层；第 2 轮起 = `sub_scenes` 顺序展开 |
| `at_bot` 缺省 | 顶层 `True`；`sub_scenes` 项 `False` |
| case 判定 | 全部轮次执行且逐轮通过；某轮失败即停止后续轮次 |
| `quote_previous=true` | 引用上一轮回复 |
| `quote_previous` 缺省 | 引用第 1 轮回复 |
| `quote_previous=false` | 不引用 |
| 引用回溯 | 上一轮为后端短错误（`正在处理`/`请勿重复`/`未授权`/`失败：未补充`，且 ≤200 字符）→ 向前回溯至最近可用回复 |

### 1.4 受影响文件盘点（23 个）

| 文件 | 现状 | 处置 |
|---|---|---|
| `harness/golden.py` | 与现役数据不兼容 | 重写 §3.1 |
| `harness/runner.py` | 单轮，state 构造过时 | 改造 §3.3 |
| `harness/differ.py` | 缺文本断言与排除键 | 改造 §3.3 |
| `harness/cli.py` | 默认路径失效、3 个 stub 子命令 | 改造 §3.4 |
| `harness/case_generator/cli.py`、`llm_paraphrase.py` | 种子与输出为旧 schema | 改造 §3.4 |
| `scripts/langfuse_eval.py` | 唯一多轮实现，与 harness 重复 | 改造 §3.5 |
| `scripts/upload_golden_to_langfuse.py` | 自有解析、旧路径 | 改造 §3.5 |
| `scripts/check_fixture_consistency.py` | 恒 `exit 2` | 重写 §3.6 |
| `scripts/probe_ticker_d2_4.py` | `--fixture` 模式数据源已归档 | 改造 §3.7 |
| `tests/test_harness.py` | 断言旧字段 | 改造 §3.1–§3.3 |
| `tests/test_fixture_consistency.py` | 仅测旧校验函数 | 重写 §3.6 |
| `tests/test_intent_route.py` | `TestGoldenRuleCoverage` 数据源已归档 | 删该类 §3.7 |
| `scripts/eval_golden.py`、`demo_closed_loop.py`、`merge_golden.py`、`convert_testcase_to_jsonl.py` | 失效 / 数据源消失 | 删除 §3.7 |
| `scripts/upload_option_dataset.py`、`upload_dataset_to_langfuse.py`、`langfuse_eval_clean.py`、`shadow_compare.py` | 失效 / 重复实现 | 删除 §3.7 |
| `tests/test_eval_golden.py`、`tests/subgraphs/ticker/test_golden_ticker_fixture.py` | 随数据源或脚本失效 | 删除 §3.7 |

---

## 2. 设计决策

### D1 · 单一归一化层：两种现役方言 → 统一内部模型；旧 schema 一律拒绝

- 仅从 `tests/fixtures/categories/*.jsonl` 加载；`old_typing/` 不读取、不兼容、不校验。
- 不做 `conversation` / `raw_content` / 旧 ticker 解析分支；出现这些键即报错，防旧格式回流。
- 方言差异在 loader 内消化，下游（runner / differ / cli / langfuse_eval / upload）只面向 `GoldenCase` + `TurnSpec`。
- 理由：格式差异只应存在一处；兼容分支没有消费方，是纯负债。

### D2 · 多轮执行器 `harness/multi_turn.py`

- 现状：多轮逻辑只存在于 `scripts/langfuse_eval.py:114-243`；harness 与其它链路单轮，行为不一致且重复实现。
- 集中实现 4 个易错点：`thread_id` 复用、引用传递（含后端短错误回溯）、轮间节流、错误早停。
- 接口与契约见 §3.2；`turn_interval` 缺省 `0`，评估节奏由 `langfuse_eval.py` 传 `EVAL_TURN_INTERVAL`（默认 1.0s）——该规避属评估层节奏问题，按根 `CLAUDE.md` P0 红线不进业务代码。

### D3 · 断言分层

| 层 | 内容 | 消费方 |
|---|---|---|
| 结构化 | `expected.product_type` / `intent` / `winners`（缺省回退 `winner`）/ `needs_hitl` | 链路 A、`langfuse_eval` |
| 文本 | `response_contains`（全含）/ `response_contains_any`（任一命中）/ `response_not_contains`（全不含） | 链路 A、`langfuse_eval` |
| 语义 | `expected.output`（自然语言期望） | 仅链路 B Judge；不参与确定性 diff |

- `differ` 增加排除键 `ASSERT_EXCLUDED_KEYS = frozenset({"output"})`，修复 P0-3。
- `response_contains_any` 参考实现按「全含」判定，与字段名及数据意图（模糊标的识别允许多个等价代码）不符 → 本项目按「任一命中」实现，落地前与数据提供方确认（附录）。

---

### D4 · 后端三档

参数：

```
--backend {mock,dry-run,real}   默认 mock
--backend-url <url>             覆盖 OTC_API_BASE_URL
--check-backend                 仅自检并打印解析结果，不执行用例
```

环境变量映射：

| 变量 | `mock` | `dry-run` | `real` |
|---|---|---|---|
| `OTC_API_BASE_URL` | `http://127.0.0.1:8099` | 沿用 `.env` | 沿用 `.env` 或 `--backend-url` |
| `GOATS_BASE_URL` | `http://127.0.0.1:8099` | 沿用 `.env` | 沿用 `.env` |
| `GOATS_CLIENT_ID` / `_SECRET` / `_EXTAPP_SALT` | `mock-*` | 沿用 `.env` | 沿用 `.env` |
| `SECURITIES_INSTRUMENT_URL` | `http://127.0.0.1:8099/admin-api/integration/securities-instrument/select` | 沿用 `.env` | 沿用 `.env` |
| `DRY_RUN_BACKEND` | `false` | `true` | `false` |
| `EVAL_USER_ID` / `EVAL_ROOM_ID` | 不要求 | 要求 | 强制校验 |

约束：

1. 必须在 import `app` 之前写 `os.environ`——`app.config.get_settings()` 为 `@lru_cache(maxsize=1)`，首次调用即固化；pydantic-settings 中环境变量优先于 `.env`。
2. `DRY_RUN_BACKEND` 复用现成实现（`app/config.py:88` → `app/tools/option_client.py:181-190`、`app/tools/swap_client.py:157`），不新增开关。
3. `mock` 模式不自动启动 `mock_api`（端口 8099）；不可达时仅打印启动命令。
4. `real` 模式前置校验 `EVAL_USER_ID` / `EVAL_ROOM_ID`，防目标后端误配。

### D5 · 入口收敛

- `harness/cli.py`：`--data` 支持多值，缺省 `discover_fixtures("tests/fixtures")`；删除 `DEFAULT_GOLDEN_PATHS`。
- `sync-golden` 委托 `scripts/upload_golden_to_langfuse.py`；`eval` 委托 `scripts/langfuse_eval.py`（复用 `_delegate_to_promote_prompt` 模式）；`diff` 移除（原为 stub，退出码 64；字段比对能力由 `harness/differ.py` 与评估报告覆盖）。
- 新增 `doctor` 子命令（banner + 连通性自检，见 §3.4）。

理由：三个 stub 是文档与实现不一致的来源（ADR 0002:41、ADR 0014:57 已记录）；委托实现成本低于独立维护。

---

## 3. 落地步骤

| 步骤 | 对象 | 方式 | 前置 |
|---|---|---|---|
| 1 | `harness/golden.py`、`tests/test_harness.py` | 重写 | — |
| 2 | `harness/multi_turn.py`、`tests/test_harness_multi_turn.py` | 新增 | 步骤 1 |
| 3 | `harness/runner.py`、`harness/differ.py` | 改造 | 步骤 1、2 |
| 4 | `harness/cli.py`、`harness/backend.py`（新增）、`harness/case_generator/*` | 改造 + 新增 | 步骤 1–3 |
| 5 | `scripts/langfuse_eval.py`、`scripts/upload_golden_to_langfuse.py` | 改造 | 步骤 1、2 |
| 6 | `scripts/check_fixture_consistency.py`、`tests/test_fixture_consistency.py` | 重写 | 步骤 1 |
| 7 | 8 个脚本、2 个测试文件、1 个测试类 | 删除 | 步骤 3、6 |

判定原则：输入契约与内部模型都变 → 重写；仅输入适配变、主体逻辑含已验证行为 → 改造；数据源消失或职责重叠 → 删除。

### 3.1 步骤 1 · 重写 `harness/golden.py`

**现状**：`GoldenCase` 为 `{id, category, expected, type, source, conversation[], raw_content, quote_content, notes}`（`:38-52`）；`load_golden` 单文件（`:55-75`），`:70-74` 复制 `conversation[0]`；实测：方言 B 抛 `ValidationError`，方言 A 全部轮次丢失。

**目标模型**：

```python
class TurnSpec(BaseModel):
    model_config = ConfigDict(extra="allow")

    scene: str = ""
    send_text: str
    at_bot: bool = False
    quote_previous: bool | None = None          # None = 未声明（→ 引用第 1 轮）
    expected: dict[str, Any] = Field(default_factory=dict)
    response_contains: list[str] = Field(default_factory=list)
    response_contains_any: list[str] = Field(default_factory=list)
    response_not_contains: list[str] = Field(default_factory=list)


class GoldenCase(BaseModel):
    model_config = ConfigDict(extra="allow")

    id: str
    category: str
    type: str = ""
    source: str = ""
    name: str = ""
    case_no: str = ""
    scene: str = ""
    turns: list[TurnSpec] = Field(default_factory=list)
    expected: dict[str, Any] = Field(default_factory=dict)   # 结构化断言
    expected_output: str = ""                                # Judge 用（来自 expected.output）
    source_path: str = ""
    source_line: int = 0
```

**目标函数**：

| 函数 | 说明 |
|---|---|
| `discover_fixtures(root: Path = Path("tests/fixtures")) -> list[Path]` | 返回 `root/categories/*.jsonl`，按文件名排序 |
| `load_golden(paths: Sequence[Path] \| Path \| None = None, *, root: Path \| None = None) -> list[GoldenCase]` | `paths` 为空时用 `discover_fixtures`；空行与 `#` 开头跳过；逐行记录 `source_path` / `source_line` |
| `normalize_case(obj: dict, *, origin: str) -> GoldenCase` | 方言判定与字段归一（规则见下），不可识别时抛 `ValueError`（信息含 `文件:行号`） |
| `validate_case(case: GoldenCase, origin: str) -> list[str]` | 静态校验：必填非空、`turns` 非空；返回错误描述列表（lint 与测试共用） |
| `index_by_category` / `filter_by_category` / `filter_by_ids` | 签名不变，元素为 `GoldenCase` |

**逐行解析规则**：

1. `json.loads` 失败 → 抛 `ValueError`，信息含 `文件:行号`。
2. 旧格式守卫：出现 `conversation` 或 `raw_content` 键 → 抛 `ValueError`。
3. 方言判定：含 `id` 或 `sub_scenes` → 方言 A；否则含 `name` + `send_text` → 方言 B；均不满足 → 报错。
4. `id`：`obj.get("id") or obj.get("caseNo") or f"{文件 stem}#{行号}"`。
5. `category`：`obj.get("category") or 文件 stem`（如 `swap_prod_data`；前缀过滤 `--category swap` 可命中三份 swap 数据）。
6. 文本断言归一：`str` → `splitlines()` 去空白行；`list[str]` → 原样。作用于 `response_contains` / `response_contains_any` / `response_not_contains`。
7. 轮次装配：
   - `turns[0]`：`scene`、`send_text`、`at_bot = obj.get("at_bot", True)`、`quote_previous = None`、`expected`、文本断言取顶层。
   - `turns[1:]`：由 `sub_scenes` 顺序构造；`at_bot` 缺省 `False`；`quote_previous` 缺省 `None`。
   - `sub_scenes` 项缺 `send_text` 或非对象 → 报错（对齐 `langgraph_direct_regression.py:257-258`）。
8. `expected_output`：取 `obj["expected"].get("output", "")`；`expected` 其余键保留供结构化断言。
9. 未识别字段由 `extra="allow"` 保留，不报错。

**测试（先写失败用例，`tests/test_harness.py`）**：

| 用例 | 断言 |
|---|---|
| `test_load_categories_total` | `load_golden()`（缺省）返回 389 条 |
| `test_option_case_turns` | `case-025`：`len(turns) == 4`（1 顶层 + 3 `sub_scenes`）；`turns[0].quote_previous is None`；`turns[1].quote_previous is True` |
| `test_dialect_b_normalized` | 任一 swap 用例：`id == caseNo`、`category` 以 `swap` 开头、`response_contains` 为多行拆分列表、`len(turns) == 1`、`expected == {}` |
| `test_empty_sub_scenes_ok` | 询价用例：`len(turns) == 1` |
| `test_rejects_legacy_schema` | 含 `conversation` 键 → `ValueError` |
| `test_rejects_missing_send_text` | 顶层缺 `send_text` → `ValueError` |
| `test_sub_scene_without_send_text` | `sub_scenes[0]` 缺 `send_text` → `ValueError` |
| `test_filter_by_category` / `test_filter_by_ids` | `filter_by_category(cases, "option")` → 13 条；`filter_by_ids(cases, ["case-025"])` → 1 条 |

### 3.2 步骤 2 · 新增 `harness/multi_turn.py`

**数据结构**：

```python
@dataclass
class TurnOutcome:
    index: int                              # 1-based
    scene: str
    send_text: str
    at_bot: bool
    quote_passed: str                       # 实际传入的引用内容（截断 120 字符）
    reply_text: str
    product_type: str
    intent: str | None
    tickers: list[dict[str, Any]]           # [{wind, desc, goats}]
    place_params: dict[str, Any] | None     # {action, orderList}
    api_code: int | None
    api_result: Any
    error: dict[str, Any] | None            # {node, type, message[:200]}
    trace: str


@dataclass
class MultiTurnResult:
    case_id: str
    conversation_id: str
    turns: list[TurnOutcome] = field(default_factory=list)
    failure: dict[str, Any] | None = None   # {turn, kind, api_code, error, reply_text}
    remaining_turns: int = 0
    final_state: dict[str, Any] = field(default_factory=dict)
```

**接口**：

```python
async def run_case_multi(
    case: GoldenCase,
    *,
    graph: Any | None = None,
    turn_interval: float = 0.0,
) -> MultiTurnResult
```

**执行步骤**：

1. `graph` 缺省 `build_main_graph(InMemorySaver())`（与现 `langfuse_eval` 一致）；测试注入 `AsyncMock`。模块仅 import `app.graph.main` / `app.state`，不 import LangFuse、Judge 相关模块。
2. `conversation_id = f"ai-test-{uuid.uuid4().hex}"`；`config = {"configurable": {"thread_id": conversation_id}}`，全程复用。
3. 逐轮构造 state：`WechatInput(conversation_id, message_id=secrets.randbelow(900_000_000_000_000) + 100_000_000_000_000, room_id=os.environ.get("EVAL_ROOM_ID", "eval-room"), user_id=os.environ.get("EVAL_USER_ID", "eval-user"), guid="", raw_content=spec.send_text, quote_content=quote)` → `make_initial_state(...)` → `state["at_bot"] = spec.at_bot` → `await graph.ainvoke(state, config=config)`。
4. 轮间节流：`if turn_interval and index > 1: await asyncio.sleep(turn_interval)`。
5. 每轮结果抽取（简化规则自 `scripts/langfuse_eval.py:114-243` 迁入）：`tickers` → `{wind, desc, goats}`；`place_params` → `{action, orderList}`（去 `None`）；`error` → `{node, type, message[:200]}`；`trace` 用 `_fmt_trace`（自 `:49-66` 迁入）；`quote_passed` 截断 120 字符。
6. 早停：`error` 非空，或 `api_code not in (None, 0)` → 写 `failure`（`kind = node_error | backend_error`）、`remaining_turns = len(case.turns) - len(turns)`，终止后续轮次。
7. 不做断言（断言属 §3.3 的 `differ` 层）。

**引用解析（`quote_previous`）**：

| 值 | 取值 |
|---|---|
| `True` | 上一轮 `reply_text`；不可用则向前回溯至最近可用回复；无可用则空 |
| `None`（未声明） | 第 1 轮为空；第 2 轮起取第 1 轮 `reply_text`（同样做可用性回溯） |
| `False` | 空 |

不可用判定（自 `langfuse_eval.py:88`、`:107-112` 迁入）：`len(text) <= 200` 且命中 `("正在处理", "请勿重复", "未授权", "失败：未补充")`。

**测试（先写失败用例，`tests/test_harness_multi_turn.py`，`AsyncMock` 注入 graph）**：

| 用例 | 断言 |
|---|---|
| `test_turn_count_and_thread_id` | `ainvoke` 调用次数 == `len(case.turns)`；各轮 `config.configurable.thread_id` 相同 |
| `test_quote_true_uses_prev_reply` | 第 2 轮 state 的 `quote_content` == 第 1 轮返回的 `reply_text` |
| `test_quote_absent_uses_first_reply` | 未声明 `quote_previous` 的第 3 轮 `quote_content` == 第 1 轮 `reply_text` |
| `test_quote_false_is_empty` | `quote_previous=False` 的轮次 `quote_content` 为空 |
| `test_backend_error_backtracks` | 第 1 轮 reply 为「正在处理，请勿重复提交」→ 第 2 轮 `quote_content` 为空（无可回溯时） |
| `test_node_error_stops_early` | 第 1 轮返回 `error` → 仅调用 1 次；`remaining_turns == len(case.turns) - 1` |
| `test_at_bot_passthrough` | state 的 `at_bot` 与 `spec.at_bot` 一致 |

### 3.3 步骤 3 · 改造 `harness/runner.py`、`harness/differ.py`

**`harness/runner.py`**

| 改动 | 说明 |
|---|---|
| 删除 | `_case_to_initial_state()`（state 构造下沉至 `multi_turn`） |
| `run_case` | 内部调 `run_case_multi(case, graph=graph, turn_interval=0.0)`；保留 callback handler 与 `TokenTracker` 注册 |
| `RunResult` | 新增 `turns: list[TurnOutcome]`；`final_state` 取末轮 state；`error` 取 `failure` 摘要 |
| 保留 | `_normalize_state`、`_to_jsonable`、异常兜底 |

**`harness/differ.py`**

| 改动 | 说明 |
|---|---|
| 新增常量 | `ASSERT_EXCLUDED_KEYS = frozenset({"output"})` |
| `diff_fields` | 增参 `exclude: Collection[str] = ASSERT_EXCLUDED_KEYS`；键命中排除集时跳过 |
| 新增函数 | `check_text_assertions(reply_text: str, spec: TurnSpec) -> list[str]`：`response_contains` 全含、`response_contains_any` 任一命中、`response_not_contains` 全不含；失败文案对齐 `regression_support.evaluate_response`（`内容包含失败：未找到 {line!r}` / `禁止包含失败：实际出现 {line!r}`） |
| 新增函数 | `check_structured_assertions(outputs: dict, expected: dict) -> list[str]`：`product_type` / `intent` / `winners`（缺省回退 `winner`）/ `needs_hitl` |

**测试**（`tests/test_harness.py` 增补）：`test_diff_fields_ignores_output`、`test_check_text_assertions_all_must_match`、`test_check_text_assertions_any_of`、`test_check_text_assertions_not_contains_hit_fails`、`test_check_structured_assertions_winners_fallback`。

### 3.4 步骤 4 · 改造 `harness/cli.py`、新增 `harness/backend.py`、同步 `case_generator`

**`harness/cli.py`**

| 改动 | 说明 |
|---|---|
| 删除 | `DEFAULT_GOLDEN_PATHS`（`:32-37`，含已不存在的 `unified_golden.jsonl`） |
| run 新参数 | `--data`（多值，缺省 `discover_fixtures()`）、`--backend`、`--backend-url`、`--check-backend`、`--turn-interval`、`--limit`；保留 `--case` / `--category` / `--out` |
| 执行顺序 | `parse_args` → `resolve_backend()` → 写 `os.environ` → import app 模块（见 §2 D4 缓存约束） |
| `cmd_run` | 按 `case.turns` 逐轮执行；逐轮 `check_text_assertions` + `check_structured_assertions`；case 通过 = 全部轮次执行且全部通过；报告沿用 `harness/reporter`（`summary.json` / `summary.md` / `failures/`） |
| 子命令 | `sync-golden` 委托 `scripts/upload_golden_to_langfuse.py`；`eval` 委托 `scripts/langfuse_eval.py`（复用 `_delegate_to_promote_prompt` 模式）；`diff` 移除 |
| 新增子命令 | `doctor`：banner + 连通性自检（与 `--check-backend` 等价） |

**`harness/backend.py`（新增）**

| 符号 | 说明 |
|---|---|
| `BackendMode = Literal["mock", "dry-run", "real"]` | 三档 |
| `resolve_backend(mode: BackendMode, url_override: str \| None = None) -> dict[str, str]` | 计算待写入的环境变量（映射见 §2 D4），无副作用 |
| `apply_backend(env: dict[str, str]) -> None` | 写入 `os.environ` |
| `doctor(mode: BackendMode) -> int` | 探测三个 URL 的连通性与延迟并打印 banner；`mock` 不可达时打印 `mock_api` 启动命令，返回 2 |
| `print_banner(mode: BackendMode, env: dict[str, str]) -> None` | 启动时打印最终生效值 |

**`harness/case_generator/`**

| 文件 | 改动 |
|---|---|
| `cli.py` | `--golden` 缺省改为 `tests/fixtures/categories`（`:164`）；种子加载改用 `load_golden` |
| `llm_paraphrase.py` | `to_golden_dict()` 输出方言 A 结构（`id` / `category` / `scene` / `send_text` / `at_bot` / `expected` / `response_contains` / `sub_scenes`）；多轮种子取 `case.turns[-1]`；生成物必须可被 `load_golden` 回读 |

### 3.5 步骤 5 · 改造 `scripts/langfuse_eval.py`、`scripts/upload_golden_to_langfuse.py`

**`scripts/langfuse_eval.py`**（行段为 2026-09-10 实测）

| 行段 | 处置 |
|---|---|
| `:49-66` `_fmt_trace` | 迁入 `harness/multi_turn.py` |
| `:68-82` `_run_graph_once`、`:84` `_TURN_INTERVAL_SECONDS` | 删除；执行改调 `run_case_multi`；`EVAL_TURN_INTERVAL` 由本脚本读取后作为 `turn_interval` 传入；同步改写 `tests/test_langfuse_eval.py`（message_id 断言迁至 `harness/multi_turn` 测试） |
| `:88` `_BACKEND_ERROR_MARKERS`、`:107-112` `_is_unusable_quote` | 迁入 `harness/multi_turn.py` |
| `:92-98` `_PASS_THRESHOLD` / `_SKIP_IMAGE_CASES` / `_IMAGE_MARKERS` | 保留（阈值与跳过策略属评估层） |
| `:101-105` `_is_image_case` | 保留；文本拼接改为按 `case.turns` |
| `:114-243` `run_langgraph_pipeline` | 删除；改为「`run_case_multi` → 组装富集 JSON → Judge」的薄封装；同步改写 `tests/test_langfuse_eval_pipeline.py`（改用 `run_case_multi` + FakeGraph） |
| `:248-330` Judge（`JUDGE` / `_parse_judge_json` / `judge_by_deepseek`） | 保留；提示词仍来自 `app/prompts/judge/option_judge.md`，不得改动口径 |
| `:331-372` `_print_report` | 保留 |
| `:374-416` `_build_local_overview` / `_LocalItem` | 删除；输入改用 `harness.golden.load_golden`，overview 用共享函数 |
| `:417-564` `run_local` | 保留；输入源改为 `--data`（目录或多文件）；`--ids` / `--filter` / `--limit` / `--concurrency` / `--dry-run` / `--no-judge` 保留 |
| `:566-600` `run_eval` / `main` | 保留；新增 `--backend` 透传 |

**必须保持不变的富集 JSON 结构**（根 `CLAUDE.md` 排查 SOP 依赖）：

```jsonc
{
  "expected": "Judge 期望输出", "score": 0.0, "judge_comment": "…",
  "turns": [{"turn": 1, "raw": "…", "reply": "…", "product_type": "option", "intent": "new_inquiry",
             "tickers": [], "place_params": null, "api_result": null, "api_code": null,
             "error": null, "trace": "…", "quote_passed": ""}]
}
```

**`scripts/upload_golden_to_langfuse.py`**

| 行段 | 处置 |
|---|---|
| `:36` `GOLDEN_PATH` | 改为 `tests/fixtures/categories` |
| `:40-49` `load_cases` | 删除；改用 `load_golden` |
| `:51-69` `_build_overview` | 迁至 `harness/reporter.py`，与 `langfuse_eval` 共用（消除双份格式实现） |
| `:71-81` `build_input` | 由 `case.turns` 生成 `{"turns": [{"raw_content", "quote_desc", "at_bot", "quote_previous"}]}`；`quote_desc` 在第 ≥2 轮且 `quote_previous is not False` 时非空 |
| `:83-85` `build_expected` | 优先 `case.expected_output`；缺省回退 `"；".join(case.turns[0].response_contains)` |
| `main()` | 保留 `--dry-run` / `--mode` / `--source` / `--dataset-name` |

### 3.6 步骤 6 · 重写 `scripts/check_fixture_consistency.py`

**待删部分**：`FILES`（`:24-32`）、`ID_PATTERNS`（`:34-42`）、`_extract_raw`（`:48-60`）、`check_unified_is_superset`（`:63-81`）、`check_anchors_present`（`:84-100`）及 `check_id_naming` 中的旧前缀规则。

**新校验项**（输入 = `categories/*.jsonl`）：

| 编号 | 校验 | 失败信息 |
|---|---|---|
| 1 | `categories/` 存在且至少 1 个 `*.jsonl` | 打印期望路径 |
| 2 | 每个文件非空；每行可 JSON 解析 | `文件:行号` |
| 3 | 归一化 `id` 跨全部文件唯一 | 列出重复 id |
| 4 | 必填：`send_text` 非空；`name` 或 `caseNo` 存在；`response_contains` 非空 | `文件:行号` + 缺失字段 |
| 5 | `sub_scenes`（存在时）每项含非空 `send_text` | `文件:行号` + 索引 |
| 6 | `category`（显式或缺省文件 stem）前缀 ∈ `{swap, option, option_close}` | 列违例值 |
| 7 | `response_contains` / `response_contains_any` / `response_not_contains` 为 `str` 或 `list[str]` | 列违例类型与位置 |
| 8 | 不得出现 `conversation` / `raw_content` 键 | 防旧格式回流 |

CLI：`--root`（缺省 `tests/fixtures`）、`--verbose`；退出码 0 通过 / 1 违反 / 2 前置缺失。

**测试**：重写 `tests/test_fixture_consistency.py`，`tmp_path` 构造最小用例覆盖上述 8 项 + 1 条全通过样本。

### 3.7 步骤 7 · 删除与文档同步

**删除清单**

| 删除对象 | 依据 / 连带动作 |
|---|---|
| `scripts/eval_golden.py` | 旧 schema + 旧端点 `/v1/message`；连带删 `tests/test_eval_golden.py` |
| `scripts/demo_closed_loop.py` | ADR 0012 已记录「patch 已删符号，不可运行」 |
| `scripts/merge_golden.py` | 归档合并流程废弃；旧校验测试由 §3.6 替换 |
| `scripts/convert_testcase_to_jsonl.py` | 输出文件不存在，输入为一次性 CSV |
| `scripts/upload_option_dataset.py` | 数据源不存在，schema 不兼容 |
| `scripts/upload_dataset_to_langfuse.py` | 旧 schema；dataset 名与主用 `otc-option-golden` 冲突 |
| `scripts/langfuse_eval_clean.py` | 与 `langfuse_eval.py` 重复实现（ADR 0005 已标记漂移风险） |
| `scripts/shadow_compare.py` | 数据源与执行路径均失效；M4 需要时基于 `run_case_multi` 重建（附录）；连带删 `tests/test_shadow_compare.py` |
| `tests/subgraphs/ticker/test_golden_ticker_fixture.py` | 数据源位于 `old_typing/` |
| `tests/test_intent_route.py::TestGoldenRuleCoverage` | 仅删该类（数据源位于 `old_typing/`），文件其余用例保留；如需规则层回归另立用例 |
| `scripts/probe_ticker_d2_4.py` 的 `--fixture` 模式 | 数据源位于 `old_typing/`；保留 `--case` / `--hitl-flow` |

**文档同步**

| 文档 | 改动 |
|---|---|
| `.github/workflows/ci.yml:43-46` | 步骤名与注释（unified ⊇ golden + 锚点 → `categories/` 校验） |
| `.claude/rules/testing.md`、`.claude/rules/git-workflow.md` | `eval_golden.py` → `python -m harness run` / `scripts/langfuse_eval.py` |
| `.claude/agents/prompt-migrator.md`、`.claude/agents/test-generator.md` | 同上 |
| `scripts/CLAUDE.md` | 删除已删脚本条目 |
| `tests/CLAUDE.md` | fixture 清单改为 `categories/` 6 文件 |
| `CLAUDE.md` | 关键命令与结构段（`--data` 缺省、`sync-golden` 委托） |
| `harness/README.md` | 多轮执行器、`--backend`、`doctor` 用法 |
| `docs/testing/README.md` | `mock` / `dry-run` / `real` 三档操作说明 |
| `tests/fixtures/README.md` | 不新建、不改写，保留现有文档 |

---

## 4. 验证与验收

### 4.1 验证命令

```powershell
# 步骤 1-3（TDD 红→绿）
.venv\Scripts\python.exe -m pytest tests/test_harness.py tests/test_harness_multi_turn.py -v

# 步骤 4
.venv\Scripts\python.exe -m harness doctor --backend mock
.venv\Scripts\python.exe -m harness run --backend mock --limit 5
.venv\Scripts\python.exe -m harness run --backend mock --case case-025
.venv\Scripts\python.exe -m harness run --backend mock --category swap --limit 10

# 步骤 5
.venv\Scripts\python.exe scripts/langfuse_eval.py --local tests/fixtures/categories --ids case-025 --no-judge
.venv\Scripts\python.exe scripts/upload_golden_to_langfuse.py --dry-run

# 步骤 6
.venv\Scripts\python.exe scripts/check_fixture_consistency.py --verbose

# 全量回归
.venv\Scripts\python.exe -m pytest tests/ -q --tb=line
```

### 4.2 验收标准

1. `load_golden()` 缺省加载 389 条；`case-025` 为 4 轮且 `turns[1].quote_previous is True`；swap 用例的 `response_contains` 已拆为逐行断言。
2. `case-025` 各轮 `thread_id` 相同；第 2 轮 `quote_content` 等于第 1 轮 `reply_text`。
3. `harness run --backend mock` 通过率不为 0，且 `expected.output` 不参与 diff。
4. `check_fixture_consistency.py` 退出码 0。
5. `langfuse_eval.py` 富集 JSON 字段名与层级与改造前一致。
6. 全量 `pytest` 无新增失败。
7. 三档 banner / 自检输出正确；`dry-run` 下写类调用被拦截。

---

## 5. 风险与回滚

| 风险 | 对策 |
|---|---|
| 排除 `output` 后链路 A 通过率上升，掩盖语义错误 | 语义由链路 B Judge 兜底；断言边界写入 `harness/README.md` |
| 376 条 swap 为生产/验收数据，可能含脏数据 | lint 只做格式校验；首轮跑分仅统计并按失败聚类处理，不阻断流程 |
| `response_contains_any` 语义与数据方预期不一致 | 按「任一命中」实现，首轮跑分后复核（附录） |
| `real` 误触发写操作 | 默认 `mock`；`dry-run` 由 `DRY_RUN_BACKEND` 拦截写类调用；`real` 强制校验 `EVAL_USER_ID` / `EVAL_ROOM_ID`；写类用例策略见附录 |
| 删除脚本影响 CI 或文档 | 删除前 `git grep` 确认引用；同步 §3.7 文档清单 |
| 3 个 swap 数据文件当前未纳入 git（untracked） | 落地前确认入库；否则干净检出下 lint 与 CI 缺数据 |

回滚：按步骤独立提交，每步可单独 revert；步骤 1–3 构成最小可用集（loader + 执行器 + 断言），步骤 4–7 为入口与清理。

---

## 6. 提交组织

按步骤分 7 次提交，`<type>(<scope>): <描述>`：

```text
refactor(harness): 重写 golden loader 为双向言归一化 + TurnSpec 模型
feat(harness): 新增 multi_turn 执行器（引用传递/节流/早停）
refactor(harness): runner/differ 接入 multi_turn 与分层断言
feat(harness): CLI 支持 --data/--backend 与后端自检
refactor(eval): langfuse_eval 与 upload 复用 harness 解析与执行层
refactor(test): 重写 fixture 一致性校验（categories 双向言）
chore(test): 删除废弃脚本与测试，同步文档
```

---

## 7. 目录归类整理（`scripts/` · `tests/`）

> 与 §3 解耦：本节的物理重组在 §3 全部落地（含 §3.7 删除项清完）后单独执行，避免「移了再删」。不影响 §3 的行为验收。

### 7.0 两层策略

| 层 | 内容 | 风险 | 时机 |
|---|---|---|---|
| 逻辑归类 | 更新 `scripts/CLAUDE.md` 分类表；`tests/CLAUDE.md` 补分类页（目录页，不涉及 fixture 文档） | 零 | 可随 §3.7 文档同步一并提交 |
| 物理重组 | 文件移入子目录 + 引用全量更新（§7.3 清单） | 中 | §3 落地后的独立 PR |

分组依据：按**运行环境与副作用**分组（离线可进 CI / 需真后端 VPN / 需评估后端 / 运维执行），比按技术栈分组更能指导「何时何地能跑」。

### 7.1 `scripts/` 目标结构（删除项清完后）

```
scripts/
├── eval/                    # 评估与数据集
│   ├── langfuse_eval.py
│   ├── upload_golden_to_langfuse.py
│   ├── run_option_eval.sh
│   └── run_real_llm_harness.sh
├── checks/                  # CI lint：离线、无副作用
│   ├── check_fixture_consistency.py
│   ├── check_adr_refs.py
│   └── check_alert_threshold_consistency.py
├── probes/                  # 真后端探针：需 VPN，不进 CI
│   ├── probe_real_backend.py / probe_real_backend_e2e.py
│   ├── probe_swap_write_e2e.py / probe_option_write_e2e.py / probe_close_write_e2e.py
│   ├── probe_ticker_e2e.py / probe_ticker_d2_4.py
│   └── probe_fast_query.py
├── ops/                     # 灰度 / 运维 / 成本
│   ├── canary_status.py / metrics_snapshot.py / run_alerts.py
│   ├── llm_cost_report.py / cleanup_checkpoints.py
│   └── rollback_canary.sh / drill_smoke.sh
├── deploy/
│   └── deploy-customer.sh
├── dev/
│   ├── dev.sh
│   └── start_backend.sh
├── prompts/                 # 提示词资产工具
│   ├── export_dify_prompts.py
│   └── promote_langfuse_prompt.py
└── ai_test_langgraph/       # 回归参考实现，整块保留
```

### 7.2 `tests/` 目标结构

```
tests/
├── harness/                 # 评测台 + fixture 消费
│   ├── test_harness.py / test_harness_multi_turn.py
│   ├── test_fixture_consistency.py
│   ├── test_case_generator.py / test_llm_paraphrase.py
│   ├── test_reporter_prompt_version.py / test_reporter_source_buckets.py / test_reporter_suspect.py
│   └── test_token_tracker.py
├── eval/                    # LangFuse / Judge
│   ├── test_langfuse_eval.py
│   └── test_langfuse_eval_pipeline.py
├── prompts/                 # 提示词治理
│   ├── test_prompt_governance.py / test_prompt_loader.py / test_prompt_versioning.py
│   └── test_promote_langfuse_prompt.py / test_langfuse_prompt_gate.py
├── scripts/                 # scripts/ 测试镜像
│   ├── test_canary_status.py / test_metrics_snapshot.py / test_rollback_canary.py
│   ├── test_drill_smoke.py / test_cleanup_checkpoints.py
│   └── test_check_adr_refs.py / test_alert_threshold_lint.py / test_probe_real_backend_e2e.py
├── nodes/ graph/ subgraphs/ integration/ observability/ tools/ api/   # 已有镜像结构，不动
├── fixtures/                # 不动
├── conftest.py              # 保持根，子目录自动继承
└── test_smoke.py            # 保留顶层（第一入口）
```

注意事项：

- `tests/api/` 被 `pyproject.toml` 的 `addopts = "--ignore=tests/api"` 整体排除——`test_api.py`（FastAPI 应用测试）**不得**移入，否则会被静默跳过。
- 杂物归位：`diagnose_close_timeout.py` → `scripts/probes/`；`test_case.md`、`业务流程.png` → `docs/`（或删除）。
- 业务测试已按 `app/` 镜像分布，本次不再调整；`test_intent_route.py`（§3.7 删类后）归 `tests/nodes/`。

### 7.3 迁移清单（2026-09-10 实测）

| 引用面 | 实测位置 | 动作 |
|---|---|---|
| 测试 import 脚本模块（16 处） | `tests/observability/test_cost_report.py`、`tests/observability/test_exit_gate_fixes.py`、`tests/test_alert_threshold_lint.py`、`tests/test_canary_status.py`、`tests/test_check_adr_refs.py`、`tests/test_cleanup_checkpoints.py`、`tests/test_fixture_consistency.py`、`tests/test_langfuse_eval.py`、`tests/test_langfuse_eval_pipeline.py`、`tests/test_metrics_snapshot.py`、`tests/test_probe_real_backend_e2e.py`、`tests/test_promote_langfuse_prompt.py`、`tests/test_prompt_governance.py`、`tests/test_rollback_canary.py`（按路径复制 `.sh`）、`tests/test_shadow_compare.py`、`tests/test_eval_golden.py` | 改 import / 路径；后两者随 §3.7 删除（见 7.4） |
| 脚本内仓库根推断（≥5 处） | `check_adr_refs.py:33`、`check_alert_threshold_consistency.py:32`、`check_fixture_consistency.py:21`、`probe_real_backend_e2e.py:37-39`、`promote_langfuse_prompt.py:31-33` | `parents[1]` → `parents[2]`；迁移前全量排查 `__file__` 用法 |
| shell 硬编码路径（4 文件） | `drill_smoke.sh`（117-121 / 143-149 / 210 / 235 / 260）、`deploy-customer.sh`（212 / 529）、`run_option_eval.sh`（6）、`rollback_canary.sh`（430） | 同步改路径 |
| CI | `.github/workflows/ci.yml:41 / 46 / 51` | 改路径 |
| 文档 / 规则 / 技能（≥10 文件） | `CLAUDE.md`、`.claude/rules/testing.md`、`.claude/rules/git-workflow.md`、`.claude/skills/{run-eval,shadow-test,iterate-option}/SKILL.md`、`scripts/CLAUDE.md`、`tests/CLAUDE.md`、`docs/m3-m4-roadmap.md`、`docs/on-call-runbook.md`、`harness/README.md` | 全文替换路径 |
| 测试内相对 fixture 路径（3-4 处） | `tests/test_intent_route.py` 等 `Path(__file__).parent / "fixtures"` 用法 | 改层级或改仓库根相对 |

### 7.4 与 §3 的交叉项（保证 §3 无残缺）

| 文件 | 原因 | 处理 |
|---|---|---|
| `tests/test_shadow_compare.py` | import `scripts.shadow_compare`，脚本随 §3.7 删除 | 随 §3.7 一并删除（§3.7 清单已补记） |
| `tests/test_langfuse_eval.py` | import §3.5 删除的 `_run_graph_once` | §3.5 同步改写（断言迁至 `harness/multi_turn` 测试） |
| `tests/test_langfuse_eval_pipeline.py` | 依赖 §3.5 删除的 `run_langgraph_pipeline` | §3.5 同步改写（改用 `run_case_multi` + FakeGraph） |
| `tests/test_fixture_consistency.py` | 现用例测 `merge_golden` | §3.6 已规划重写 |

### 7.5 命名约定（固化）

- `scripts/`：动词前缀 `check_*`（lint）/ `probe_*`（真后端）/ `upload_*` / `run_*`（封装）；子目录按运行环境分组（7.1）。
- `tests/`：文件名 = 被测模块名；目录镜像来源（`tests/scripts/` 镜像 `scripts/`、`tests/harness/` 镜像 `harness/`）；新增脚本必须带同名测试。
- 包结构：迁移时给 `scripts/` 与各子目录加 `__init__.py`（当前为隐式命名空间包），import 统一为 `scripts.<group>.<module>`；`python scripts/<group>/<name>.py` 直接执行不受影响。

### 7.6 执行顺序与验收（独立 PR）

1. 前置：工作区干净、§3 全部合入。
2. 移 `scripts/`：建目录 → `git mv` → 改测试 import（§7.3 第 1 行）→ 改 `parents` 深度 → 改 shell / CI。
3. 移 `tests/`：`git mv` → 改相对 fixture 路径。
4. 文档同步：§7.3 第 5 行清单 + `scripts/CLAUDE.md` / `tests/CLAUDE.md` 分类表重写。

```powershell
# 旧路径引用清零（示例；按 §7.3 清单逐项执行，无输出即通过）
git grep -n -E "scripts/(check_fixture_consistency|check_adr_refs|canary_status|metrics_snapshot)"
git grep -n -E "^from scripts\.|^from scripts import|sys\.path.*scripts"
# 全量回归 + 守门
.venv\Scripts\python.exe -m pytest tests/ -q --tb=line
.venv\Scripts\python.exe scripts/checks/check_fixture_consistency.py --verbose
```

完成判据：旧路径引用为零、全量测试通过、CI 绿、两份 `CLAUDE.md` 分类表与新结构一致。

### 7.7 风险

| 风险 | 对策 |
|---|---|
| 未提交改动与移动冲突（工作区常有 stash/pop） | 迁移前确认工作区干净、开单独分支 |
| 引用漏改（shell 与文档最易漏） | 以 `git grep` 清零为准入条件，不靠人工清点 |
| 加 `__init__.py` 改变导入语义 | 一次性全改；`pyproject` 已配 `pythonpath=["."]`，包路径兼容 |
| 与 §3 删除项叠加 | 先删（§3.7）后移（§7），避免「移了再删」 |

---

## 附录 · 待决策

| 编号 | 事项 | 建议 |
|---|---|---|
| 1 | `response_contains_any` 语义：任一命中（数据意图）vs 参考实现的全命中 | 按「任一命中」实现，首轮跑分后与数据提供方复核 |
| 2 | `scripts/shadow_compare.py`：本轮删除或重建 | 删除；M4 灰度需要时基于 `run_case_multi` 重建 |
| 3 | `real` 模式是否允许写类用例；若允许是否加 `--allow-write` + 专用测试对手 | M3.3 现场联调前排定 |
| 4 | 3 个 swap 数据文件的入库方式（当前 untracked） | 落地前 `git add`；或移出 `categories/` 并显式传入 `--data` |
