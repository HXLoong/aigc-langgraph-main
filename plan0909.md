# plan0909 · golden 测试脚本改造方案

- 日期：2026-09-09
- 范围：读写 `tests/fixtures/*.jsonl` 的脚本（排除 `scripts/ai_test_langgraph/**`）
- 依据：全仓检索 + 逐文件读码 + fixture 实测盘点（详见 §1）
- 约束：不改 `app/` 业务代码，不改提示词，不修改 fixture 数据内容

---

## 1. 现状

### 1.1 fixture

| 指标 | 数值 |
|---|---|
| fixture 文件 | 10 个 |
| 用例总数 | 975 条 |
| 多轮用例 | 319 条（主集 `golden.jsonl` 占 184 条） |
| 零引用 fixture | 8 个文件 / 406 条（41.6%） |
| 被引用但不存在 | 4 个（`unified_golden.jsonl`、`option_golden.jsonl`、`golden_business_seeds_2026-05.jsonl`、`README.md`） |

主 schema 为 unified：`{id, category, type, source, conversation: [{raw_content, quote_desc}], expected}`。
例外：`golden_ticker_2026-05.jsonl` 使用旧 schema `{id, category, raw_content, expected, notes}`。

### 1.2 脚本

| 状态 | 数量 | 分布 |
|---|---|---|
| 失效 | 8 | `scripts/` 8 个 |
| 半失效 | 4 | `harness/` 3 个、`tests/` 1 个 |
| 正常 | 5 | `scripts/` 3 个、`tests/` 2 个 |

### 1.3 已确认缺陷

| 编号 | 缺陷 | 证据 |
|---|---|---|
| P0-1 | `python -m harness run` 对主集必然全部 FAIL：`expected.output` 为自然语言期望，`AgentState` 无 `output` 字段（仅 `reply_text`），`diff_fields` 每条至少产生 1 个 diff | `harness/cli.py:65-68`、`harness/differ.py:70-73`、`app/graph/state.py:171` |
| P0-2 | harness 链路对 184 条多轮用例仅执行第 1 轮 | `harness/golden.py:70-74` 复制 `conversation[0]` 至顶层字段；`harness/runner.py` 单次 `ainvoke` |
| P1-1 | `scripts/check_fixture_consistency.py` 必然 `exit 2`；补齐缺失文件后锚点校验仍失败（`golden.jsonl` 已无 `g001-g030`，实际 id 为 `swap-001`/`opt-001`） | `:24-28`、`:34`、`:81-92`、`:124-128` |
| P1-2 | `tests/test_intent_route.py::test_rule_layer_precision` 将占位 `quote_desc` 作为 `quote_content`，≥80% 阈值对引用场景无效 | `docs/archive/reports/intent-recognition-assessment-2026-08.md:40` |
| P2-1 | 文档与实现不符 ≥8 处（`eval_golden.py` 被多处推荐、`langfuse_eval_clean.py` 被描述为清理脚本等） | 见 §9 |

---

## 2. 目标

1. **多轮**：保留链路全部支持 `conversation` 全轮执行，行为一致。
2. **后端**：支持 `mock` / `dry-run` / `real` 三档，命令行切换，启动自检。
3. **精简**：删除失效脚本、重复实现与与测试目的无关的逻辑。

非目标：不新增业务能力；不改动 `app/` 与 `app/prompts/`；不重新标注 fixture 内容。

---

## 3. 处置清单

### 3.1 保留并改造（9）

| 脚本 | 改造要点 |
|---|---|
| `harness/golden.py` | 移除字段复制；`conversation` 为唯一数据源；新增校验 |
| `harness/runner.py` | 接入多轮执行器；`RunResult` 增 `turns` |
| `harness/cli.py` | 增 `--data` / `--backend`；stub 子命令委托实现 |
| `harness/differ.py` | 断言排除 `output` |
| `harness/case_generator/llm_paraphrase.py` | 输出改为 unified schema |
| `scripts/langfuse_eval.py` | 复用多轮执行器；删除自有 pipeline |
| `scripts/upload_golden_to_langfuse.py` | 复用 loader 与 overview |
| `scripts/probe_ticker_d2_4.py` | 数据路径参数化；增 `--backend` |
| `tests/test_intent_route.py` | 复用 loader；修复 quote 口径 |

### 3.2 保留（2）

`tests/subgraphs/ticker/test_golden_ticker_fixture.py`、`tests/test_harness.py`（后者仅补断言）。

### 3.3 新增（3）

`harness/multi_turn.py`、`harness/backend.py`、`tests/test_harness_multi_turn.py`。

### 3.4 删除（7 脚本 + 1 测试）

| 路径 | 依据 |
|---|---|
| `scripts/eval_golden.py` | 旧 schema（`case["raw_content"]`）、旧端点 `/v1/message`；对 unified 必然 `KeyError` |
| `scripts/demo_closed_loop.py` | ADR 0012:32 已记录「仍 patch 已删符号，已不可运行」 |
| `scripts/merge_golden.py` | 输入/输出文件均已不存在 |
| `scripts/convert_testcase_to_jsonl.py` | 输出文件已不存在；输入为一次性 `/tmp/*.csv` |
| `scripts/upload_option_dataset.py` | 数据源已不存在；schema 与 unified 不兼容 |
| `scripts/upload_dataset_to_langfuse.py` | 旧 schema；dataset 名 `otc-agent-golden` 与主用 `otc-option-golden` 冲突 |
| `scripts/langfuse_eval_clean.py` | 与 `langfuse_eval.py` 重复实现（ADR 0005:23 已标记漂移风险） |
| `tests/test_eval_golden.py` | 随 `eval_golden.py` 删除 |

### 3.5 重建（2）

| 路径 | 方式 |
|---|---|
| `scripts/shadow_compare.py` | 基于 `run_case_multi` 重写 LangGraph 侧；Dify 侧保留 HTTP 调用；字段 diff 复用 `harness/differ.py`；默认 `--backend dry-run` |
| `scripts/check_fixture_consistency.py` | 重写校验集（见 §5.11） |

### 3.6 fixture 处置

8 个孤儿 fixture 暂移入 `tests/fixtures/_orphan/` 并附 README 说明来源，不直接删除；由业务方确认后合并进 `golden.jsonl` 或废弃。

---

## 4. 核心设计

### D1 · `conversation` 作为唯一数据源

**问题**：`harness/golden.py:70-74` 将 `conversation[0]` 的 `raw_content` / `quote_desc` 复制到对象顶层字段。下游 `harness/runner.py:_case_to_initial_state` 仅读顶层字段，`conversation[1:]` 无任何消费方，导致 184 条多轮用例的第 2 轮及以后数据未被使用。

**动作**：

1. 删除 `harness/golden.py:70-74` 的赋值逻辑。
2. `raw_content` / `quote_content` 改为只读属性，从 `conversation[0]` 派生，标注 `deprecated`：

```python
class GoldenCase(BaseModel):
    conversation: list[ConversationTurn] = Field(default_factory=list)

    @property
    def raw_content(self) -> str:
        """deprecated：等价 conversation[0].raw_content，仅供既有调用方。"""
        return self.conversation[0].raw_content if self.conversation else ""
```

3. 新增 `iter_turns()`；`load_golden()` 支持多路径入参。
4. 新增 `validate()`：`conversation` 非空、每轮 `raw_content` 非空、`expected.product_type` 与 `expected.intent` 存在。

**理由**：字段复制会产生两份数据，二者必然不一致；派生属性保证单一真源，同时保持既有 5 个调用方（`harness/cli.py`、`harness/runner.py`、`harness/case_generator/cli.py`、`harness/case_generator/llm_paraphrase.py`、`tests/test_harness.py`）无需同步修改。

### D2 · 多轮执行器 `harness/multi_turn.py`

**问题**：多轮逻辑仅存在于 `scripts/langfuse_eval.py:113-243`；`harness/runner.py` 与 `shadow_compare.py` 均为单轮，行为不一致且重复实现。

**接口**：

```python
async def run_case_multi(
    case: GoldenCase,
    *,
    graph: Any | None = None,                 # 默认 build_main_graph(InMemorySaver())
    turn_interval: float = 0.0,               # 轮间休眠，默认 0
    quote_policy: Literal["prev", "prev_skip_backend_error", "none"] = "prev_skip_backend_error",
    stop_on_backend_error: bool = True,
) -> MultiTurnResult
```

**行为契约**：

1. 单个 `conversation_id` 作为 `thread_id`，全程复用。
2. 按 `conversation` 顺序逐轮执行，每轮重新构造 `WechatInput` 与 `make_initial_state`。
3. `quote_desc` 非空时传入引用内容：默认取上一轮 `reply_text`；若上一轮为后端短错误（`正在处理`/`请勿重复`/`未授权`/`失败：未补充`）则回溯至更早的可用回复。
4. 出现节点 `error` 或 `api_code != 0` 时记录 `failure` 并终止后续轮次，`remaining_turns` 记录未执行轮数。
5. 输出 `TurnOutcome` 列表，字段：`index`、`raw_content`、`quote_desc`、`reply_text`、`product_type`、`intent`、`tickers`、`place_params`、`api_code`、`api_result`、`error`、`trace`。

**理由**：多轮涉及 4 个易错点（`thread_id` 复用、引用传递、后端去重间隔、错误早停）。集中实现后，链路 A/B 与重建后的 `shadow_compare` 共用同一执行路径，行为可对齐；后续修复只需改一处。

**边界**：`turn_interval` 默认 0；评估场景由 `langfuse_eval.py` 传入 `EVAL_TURN_INTERVAL`（默认 1.0s）。该规避属于评估节奏问题，按根 `CLAUDE.md` P0 红线仅保留在评估层。

### D3 · 断言口径排除 `output`

**问题**：`expected.output` 为自然语言期望，`AgentState` 无对应字段，`diff_fields` 对其必然产生 diff，导致 harness 全部 FAIL。

**动作**：`harness/differ.py` 增加常量并在比对时排除：

```python
ASSERT_EXCLUDED_KEYS = frozenset({"output"})
```

**理由**：确定性断言应只覆盖状态字段（`product_type` / `intent` / `tickers` / `place_params`）；语义正确性由链路 B 的 Judge 负责。两条链路的断言边界需在文档中明确。

### D4 · 后端三档参数

**现状**：不存在任何命令行参数。切换后端需手工修改 `.env` 中 6 个变量，无校验、无提示。

| 现有开关 | 位置 |
|---|---|
| `OTC_API_BASE_URL` | `app/config.py:33` |
| `GOATS_BASE_URL` | `app/config.py:37` |
| `GOATS_CLIENT_ID` / `_SECRET` / `_EXTAPP_SALT` | `app/config.py:38-40` |
| `SECURITIES_INSTRUMENT_URL` | `app/config.py:47` |
| `DRY_RUN_BACKEND` | `app/config.py:88` → `app/tools/option_client.py:181-190`、`app/tools/swap_client.py:157` |
| `mock_api` 服务 | `mock_api/server.py`，端口 8099 |

**参数**：

```
--backend {mock,real,dry-run}   默认 mock
--backend-url <url>             覆盖 OTC_API_BASE_URL
--check-backend                 仅自检并打印解析结果，不执行用例
```

**环境变量映射**：

| 变量 | `mock` | `dry-run` | `real` |
|---|---|---|---|
| `OTC_API_BASE_URL` | `http://127.0.0.1:8099` | 沿用 `.env` | 沿用 `.env` 或 `--backend-url` |
| `GOATS_BASE_URL` | `http://127.0.0.1:8099` | 沿用 `.env` | 沿用 `.env` |
| `GOATS_CLIENT_ID` / `_SECRET` / `_EXTAPP_SALT` | `mock-*` | 沿用 `.env` | 沿用 `.env` |
| `SECURITIES_INSTRUMENT_URL` | `http://127.0.0.1:8099/admin-api/integration/securities-instrument/select` | 沿用 `.env` | 沿用 `.env` |
| `DRY_RUN_BACKEND` | `false` | `true` | `false` |
| `EVAL_USER_ID` / `EVAL_ROOM_ID` | 不要求 | 要求 | 强制校验 |

**自检输出**（`harness doctor --backend mock`）：

```
backend   : mock
OTC_API   : http://127.0.0.1:8099                        [OK 12ms]
GOATS     : http://127.0.0.1:8099                        [OK  9ms]
SECURITY  : .../securities-instrument/select             [OK 11ms]
DRY_RUN   : false
EVAL_USER : (未设置，mock 模式不要求)
```

**实现约束**：

1. 必须在 import `app` 之前写入 `os.environ`。`app.config.get_settings()` 带 `@lru_cache(maxsize=1)`，首次调用即固化；pydantic-settings 优先级为「环境变量 > `.env`」，因此写入 env 可覆盖 `.env`。
2. `mock` 模式不自动启动 `mock_api`；检测到端口不可达时仅输出启动命令。

**理由**：`DRY_RUN_BACKEND` 已在 `app/tools/*_client.py` 实现并经验证（`docs/on-call-runbook.md §5.7`），复用它可避免新增开关；显式参数与前置校验可防止「目标后端与预期不符」的误配，该校验方式已在 `scripts/probe_real_backend_e2e.py` 使用。

### D5 · 入口收敛

`harness/cli.py`：

- `--data`（可多次，替代 `DEFAULT_GOLDEN_PATHS`，并移除已不存在的 `unified_golden.jsonl`）。
- `sync-golden` 委托 `scripts/upload_golden_to_langfuse.py`；`eval` 委托 `scripts/langfuse_eval.py`（复用既有 `_delegate_to_promote_prompt` 模式）。
- `diff` 实现或移除子命令（当前为 stub，退出码 64）。

**理由**：三个 stub 是文档与实现不一致的来源（ADR 0002:41、ADR 0014:57 已记录）；委托实现成本低。

---

## 5. 逐脚本改造细则

### 5.1 `harness/golden.py`

- 删除 `:70-74` 字段复制。
- `raw_content` / `quote_content` 改为派生属性，加 `deprecated` 说明。
- 新增 `iter_turns()`、`validate()`。
- `load_golden(paths: Sequence[Path] | Path)` 支持多路径。
- 更新模块 docstring：移除「option_golden.jsonl M1 阶段不消费」等过期描述，改为「unified schema，`conversation` 为唯一轮次来源」。

### 5.2 `harness/multi_turn.py`（新增）

- 实现 D2 接口与行为契约。
- 不 import LangFuse、Judge 相关模块。
- 单元可测：`graph` 可注入。

### 5.3 `harness/runner.py`

- 删除 `_case_to_initial_state()`；`WechatInput` / `make_initial_state` 构造下沉至 `multi_turn`。
- `run_case()` 内部调用 `run_case_multi(turn_interval=0)`。
- `RunResult` 新增 `turns: list[TurnOutcome]`；`final_state` 取最后一轮状态，保持 reporter / differ 兼容。

### 5.4 `harness/differ.py`

- 新增 `ASSERT_EXCLUDED_KEYS = frozenset({"output"})`。
- `diff_fields(..., exclude: Collection[str] = ASSERT_EXCLUDED_KEYS)`。

### 5.5 `harness/cli.py`

- 移除 `DEFAULT_GOLDEN_PATHS` 中 `unified_golden.jsonl`（`:36`）。
- 新增 `--data`（append）、`--backend`、`--backend-url`、`--turn-interval`、`--limit`。
- 执行顺序：`parse_args` → `resolve_backend()` → 写 env → import app。
- `sync-golden` / `eval` 委托实现；`diff` 实现或移除。
- 启动打印 backend banner。

### 5.6 `harness/case_generator/llm_paraphrase.py`

- `to_golden_dict()` 输出 unified schema：`conversation: [{raw_content, quote_desc}]`，移除顶层 `raw_content` / `quote_content`。
- `paraphrase_case()` 多轮种子取 `seed.conversation[-1]`。
- 理由：生成物需可被 `load_golden` 直接回读。

### 5.7 `scripts/langfuse_eval.py`

- 删除 `run_langgraph_pipeline`、`_run_graph_once`、`_is_unusable_quote`、`_TURN_INTERVAL_SECONDS`，改调 `harness.multi_turn`。
- 删除自有 jsonl 解析与 `_build_local_overview`，改用 `load_golden` 与共享 overview 函数。
- 保留：`judge_by_deepseek`、LangFuse 写分、报告输出、`--ids` / `--filter` / `--limit` / `--concurrency` / `--dry-run` / `--no-judge`。
- 新增 `--backend` 透传。

### 5.8 `scripts/upload_golden_to_langfuse.py`

- 删除自有 `load_cases`、`_build_overview`。
- 改用 `load_golden`，`build_input` 读取 `case.conversation`。
- 理由：消除与 `langfuse_eval._LocalItem` 的双份格式实现（当前仅靠 docstring 约定一致）。

### 5.9 `scripts/probe_ticker_d2_4.py`

- `Path("tests/fixtures/golden_ticker_2026-05.jsonl")`（`:121`）改为 `--data` 参数。
- 新增 `--backend` 与 banner（三种模式均为只读，可安全执行）。

### 5.10 `tests/`

| 文件 | 动作 |
|---|---|
| `tests/test_harness.py` | 删除 `assert all(c.raw_content)`；新增 `test_load_golden_preserves_multi_turn`（断言 `len(conversation) > 1` 且 `raw_content == conversation[0].raw_content`）、`test_diff_fields_ignores_output` |
| `tests/test_intent_route.py` | `_load_golden()` 改用 `harness.golden.load_golden`；改取 `case.conversation[-1]`；引用场景改用上一轮真实回复（修复 P1-2） |
| `tests/subgraphs/ticker/test_golden_ticker_fixture.py` | 路径改为可覆盖常量，其余不变 |
| `tests/test_harness_multi_turn.py`（新增） | TDD 先写：2 轮用例（`swap-135`）断言 `len(turns) == 2` 且第 2 轮 quote 等于第 1 轮 reply；覆盖 dedup 回溯、节点 error 早停、`quote_desc` 为空不传 quote。使用 `AsyncMock` 注入 graph，不调用真实 LLM |

### 5.11 `scripts/check_fixture_consistency.py`（重建）

校验项：

1. 文件存在性。
2. `load_golden` 解析成功。
3. id 唯一。
4. schema 必填字段完整（`id` / `category` / `conversation` / `expected.product_type` / `expected.intent`）。
5. `category` 前缀在白名单内（`swap` / `option` / `option_close` / `query` / `unknown`）。
6. `conversation` 非空且每轮 `raw_content` 非空。

移除已失效的 `unified ⊇ golden` 与 `g001-g030` 锚点校验。

### 5.12 `scripts/shadow_compare.py`（重建）

- LangGraph 侧改用 `run_case_multi`。
- Dify 侧保留 HTTP 调用与 `_normalize_dify`。
- 字段级 diff 复用 `harness/differ.py`。
- 默认 `--backend dry-run`；`real` 需显式指定。

---

## 6. 实施顺序

```
阶段 1 · 数据层（无行为变更）
  harness/golden.py → upload_golden_to_langfuse.py → tests/test_harness.py

阶段 2 · 执行层（修复 P0）
  harness/multi_turn.py（新增）→ harness/runner.py → tests/test_harness_multi_turn.py
                              → scripts/langfuse_eval.py → probe_ticker_d2_4.py

阶段 3 · 断言与入口
  harness/differ.py → harness/cli.py → harness/backend.py（新增）→ case_generator

阶段 4 · 清理与重建
  删除 §3.4 → 重建 §3.5 → 文档同步 §9 → fixture 归档 §3.6
```

阶段 1、2 为强依赖（多轮执行器依赖 loader 改造）；阶段 3 可与阶段 2 并行。

---

## 7. 验证与验收

### 7.1 验证命令

```powershell
# TDD 红→绿
.venv\Scripts\python.exe -m pytest tests/test_harness_multi_turn.py -v

# 全量回归
.venv\Scripts\python.exe -m pytest tests/ -q --tb=line

# 链路 A：mock
.venv\Scripts\python.exe -m harness run --data tests/fixtures/golden.jsonl --backend mock --case swap-135
.venv\Scripts\python.exe -m harness run --data tests/fixtures/golden.jsonl --backend mock --limit 10

# 链路 B：本地 Judge（先关闭 Judge 验证多轮）
.venv\Scripts\python.exe scripts/langfuse_eval.py --local tests/fixtures/golden.jsonl --ids swap-135 --no-judge

# 链路 C
.venv\Scripts\python.exe -m pytest tests/test_intent_route.py tests/subgraphs/ticker/ -v
.venv\Scripts\python.exe scripts/probe_ticker_d2_4.py --fixture

# 真后端安全档
.venv\Scripts\python.exe -m harness run --backend dry-run --case swap-001
# 确认后执行只读用例
.venv\Scripts\python.exe -m harness run --backend real --case swap-001

# 守门人
.venv\Scripts\python.exe scripts/check_fixture_consistency.py
```

### 7.2 验收标准

1. `swap-135` 在链路 A 输出 2 个 turn（当前为 1）。
2. 链路 A 在 mock 后端下通过率不再恒为 0。
3. `check_fixture_consistency.py` 退出码 0。
4. 全量 `pytest` 无新增失败。
5. `--backend mock` / `dry-run` / `real` 三档 banner 与自检输出正确。

---

## 8. 风险与回滚

| 风险 | 对策 |
|---|---|
| 排除 `output` 后链路 A 通过率上升，掩盖语义错误 | 语义正确性由链路 B 的 Judge 兜底；文档明确两条链路断言边界 |
| `real` 模式误触发真实下单 | 默认 `mock`；`real` 强制校验 `EVAL_USER_ID` / `EVAL_ROOM_ID`；写类用例需 `--allow-write` |
| 删除脚本影响 CI 或文档 | 删除前全仓 `git grep` 确认；同步 §9 文档 |
| 孤儿 fixture 归属未定 | 移入 `tests/fixtures/_orphan/`，不删除 |

回滚方式：改造按阶段提交，每阶段独立可回滚；`harness/golden.py` 的派生属性保证阶段 1 单独合入时行为不变。

---

## 9. 文档同步

| 文档 | 改动 |
|---|---|
| `.claude/rules/testing.md:52`、`.claude/rules/git-workflow.md:72` | `eval_golden.py` → `langfuse_eval.py` 或 `python -m harness run` |
| `.claude/agents/prompt-migrator.md:60`、`.claude/agents/test-generator.md:98` | 同上 |
| `scripts/CLAUDE.md:12/13/16/59-61` | 删除已删脚本条目；修正 `langfuse_eval_clean.py` 描述 |
| `tests/CLAUDE.md:11` | 移除 `README.md` 引用，补 10 个 fixture 清单 |
| `CLAUDE.md:33` | `sync-golden` 已非 stub |
| `harness/README.md` | 补多轮执行器与 `--backend` 用法 |
| `docs/testing/README.md` | 补 `mock` / `dry-run` / `real` 三档操作说明 |
| `tests/fixtures/README.md` | 新建，恢复职责矩阵与孤儿清单 |

---

## 10. 待决策

1. 孤儿 fixture（406 条）最终处置：合并进 `golden.jsonl` / 长期保留在 `_orphan/` / 废弃。
2. `real` 模式是否允许执行写类用例；若允许，是否采用「显式 `--allow-write` + 专用测试交易对手」。
3. `shadow_compare.py` 本轮重建，或先删除、M4 阶段再建。
