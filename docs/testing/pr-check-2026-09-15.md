# PR 检查记录 · 2026-09-15

## 范围与结论

按 `.claude/rules/git-workflow.md` 的 PR 检查清单，检查 `feature-hxl` 当前工作区
相对 `HEAD=c680aeb` 的未提交变更，包含新增的全新下单交易对手节点。

本轮继续修复五项 Review 缺陷及工作台的两项 Ruff 告警，保留开始时工作区的全部改动。
本地自动化与真实业务验收分别记录；真实 Golden 准确率及客户 GOATS 业务闭环尚未完成。

## 五项 Review 缺陷修复

| 项目 | 最终行为 | RED → GREEN 证据 |
| --- | --- | --- |
| P1 撤单范围 | 从引用订单块关联订单号、编号、标的代码／名称和合约编号；同类选择合并、不同限定取交集，按订单去重。明确编号按引用编号匹配，无编号按首次出现顺序。无法确定时抛 `CancelScopeError`，节点清空撤单参数并提示补充订单号／重新引用，后端零调用 | 先复现两笔订单只撤第一笔却提交全部；再覆盖标的／合约、编号与引用位置不同、同一行多笔、重复订单、全部撤单及越界／冲突／未知限定。通过真实撤单节点、DTO、HTTP mock 载荷和渲染验证 |
| P2 当轮输入残留 | API 保留别名冲突检查与正文回填，再补 `fast_query`、`existing_command`、`at_bot`、引用字段的 `None`、附件空列表和正文空串；显式值优先，业务参数／tickers／历史消息不补空 | 首轮快速询价、存量指令或图片后，次轮省略输入的 3 个真实 API＋主图＋InMemorySaver 场景均先复现错误路由；修复后正常路由、业务上下文保留及显式输入测试通过 |
| P2 完整证券代码回退 | 输入中的完整代码优先单独查询；数字前导零变体保持同一交易所，响应也按完整身份验证。派生裸数字只合并已验证来源词，不再触发模糊回退；其他独立标的照常解析 | 先复现 `000858.SH` 返回 `000858.SZ`；通过公开 resolver＋真实 ticker client＋HTTP mock 验证有效代码、裸数字、逆序 LLM 候选、混合标的、错误交易所响应、前导零及来源词去重 |
| P2 任一包含断言 | 复用现有候选文本归一化，任一子串命中即通过；全部未命中只报一次组级失败；空列表无约束，全部包含／全部不包含语义保留 | 换行字符串和列表候选中只有一项命中的 2 项测试先失败，修复后包含空值与三种断言组合的测试通过 |
| P2 默认分类数据 | CLI 不传 `--data` 时按文件名排序加载 `categories/` 直属 JSONL；工作台发现及两项自检同步使用分类数据；不扫描嵌套目录或历史归档，显式多文件与三种格式兼容 | 先复现默认 CLI、工作台发现、自检及原有 3 项脚本测试失败；修复后真实分类数据、临时嵌套／归档目录和显式多文件测试通过 |

两处进程清理的 `try/except OSError/pass` 改为 `contextlib.suppress(OSError)`，
只包住原先的 `terminate()`／`kill()` 调用，保留外层异常处理范围。

联合回归还暴露一项既有测试的环境隔离问题：
`test_case_trace_is_disabled_outside_development` 的配置被前序评估脚本导入后写入的
进程环境覆盖。该测试现在局部隔离环境变量，继续检查 production 下即使启用
LangFuse 也不创建客户端；未改产品行为、未降低原断言。

## 本轮联合验收结果

**本地自动化：通过。** 五项 Review 缺陷均完成失败测试→最小修复→定向通过→全量回归。
相对本轮基线新增 91 项测试，修正 3 项失效脚本测试及 1 项既有测试的环境隔离。

| 最终检查 | 结果 |
| --- | --- |
| 联合 `pytest tests/ scripts/ai_test_langgraph/ -v -W error -rs` | **1899 passed、15 skipped**，92.42 秒，零失败、零警告，退出码 0 |
| `ruff check app/ tests/ scripts/ai_test_langgraph/` | **All checks passed!**，零告警，退出码 0 |
| 默认 `--dry-run --limit 3` | 加载 **389 条**，选中 `case-030`、`case-031`、`case-032`，退出码 0 |
| 直接回归脚本 `--self-test` | **5/5** 通过，包含默认分类数据加载，退出码 0 |
| 工作台 `--self-test` | **17/17** 通过，退出码 0；Windows 不支持进程组暂停时沿用原有明确降级检查 |
| `git diff --check` | 通过 |

保留仓库 `tests/api` 的原有排除及 15 项条件跳过：14 条 ticker fixture 没有 `winner`
字段，1 项 Windows 不支持 chmod；本轮未新增 skip／xfail，也未调整警告过滤或降低断言。
完整联合 pytest 输出保存在本地 `tmp/review-fixes-pytest-2026-09-15.log`（临时文件不提交）。

默认数据目录：`tests/fixtures/categories/`，6 份、389 条顶层用例：

| 文件 | 条数 |
| --- | ---: |
| `golden_option_close_case.jsonl` | 4 |
| `golden_option_inquiry_case.jsonl` | 4 |
| `golden_option_open_case.jsonl` | 5 |
| `swap_prod_acceptance_data.jsonl` | 163 |
| `swap_prod_data.jsonl` | 123 |
| `swap_test_fuzzy_target_recog_data.jsonl` | 90 |

```powershell
$env:PYTHONUTF8 = '1'
$env:ENABLE_LANGFUSE = 'false'
.\.venv\Scripts\python.exe -m pytest tests/ scripts/ai_test_langgraph/ -v -W error -rs
.\.venv\Scripts\python.exe -m ruff check app/ tests/ scripts/ai_test_langgraph/
.\.venv\Scripts\python.exe scripts/ai_test_langgraph/langgraph_direct_regression.py --dry-run --limit 3
.\.venv\Scripts\python.exe scripts/ai_test_langgraph/langgraph_direct_regression.py --self-test
.\.venv\Scripts\python.exe scripts/ai_test_langgraph/automation_runner_server.py --self-test
```

**真实业务验收：待验证。** 本轮新增测试只替换外部 LLM、HTTP 和数据库边界，
业务节点、主图、状态合并、客户端 DTO 与请求构造保持真实。HTTP mock 的成功响应、
数据加载和离线自检不计作真实模型准确率、本地 Java→GOATS 链路 PASS 或业务交易 PASS。
未在真实后端执行下单／撤单，未取得 GOATS 真实成功与后续订单可追踪的证据。

以下保留此前 PR 检查阶段的结果，常规测试数量以本轮联合验收为准。

## 此前逐项检查记录

| 检查项 | 结果与依据 |
| --- | --- |
| `pytest tests/ -v` | 最终复验 1782 passed、15 skipped，耗时 95.30 秒，零失败、零警告；额外使用 `-W error`，任何未处理警告都会使测试失败 |
| `ruff check app/ tests/` | 通过；本次修改的两个脚本也单独通过 Ruff |
| 提示词加载变更的 Golden 准确率不低于上一版 | 待验证。新增节点调用现有 `load_prompt()`，加载器及版本解析实现未改；新增模板与 Dify 原文一致的测试通过，但不能据此判断真实模型准确率 |
| 新增节点／意图配套 golden case | 新增 `tests/fixtures/golden_swap_fresh_counterparty.jsonl`，保存并由现有节点测试直接消费 6 条工程回归样例；本次无新增意图枚举 |
| State 与 `make_initial_state()` 同步 | State 类型无变更；本轮 API 入口补齐当轮输入默认值，复用既有字段，业务上下文仍由 checkpoint 保留 |
| `pyproject.toml` 依赖变更说明 | 不适用：`pyproject.toml` 和 `uv.lock` 均无变更 |
| 无硬编码 secret | 工作区变更文件的凭据字面量、常见令牌及私钥模式扫描无命中；变更列表无 `.env`、缓存或临时输出 |
| 中文变更说明 | 见本报告及下方可用于 PR 的变更说明 |

## 此前完成的修复

1. 历史基准数据已迁入 `tests/fixtures/old_typing/`，原有消费者仍读旧路径。
   修正 ticker fixture 测试、harness 加载测试、规则精准率测试、一致性检查、
   合并脚本及 3 份 ADR 的引用，保留原有数据与断言。
2. 从既有测试中整理 6 条全新交易对手节点 golden 样例，覆盖完整名称／简称、
   单笔／多笔补全、已有相同名称的订单，并继续检查其余字段及输入 State 不变。
   这些是固定 LLM 响应下的节点回归，不计入真实模型准确率。
3. 删除 `scripts/merge_golden.py` 的未使用 `sys` 导入。
4. 测试进程使用与 `.github/workflows/ci.yml` 一致的 `ENABLE_LANGFUSE=false`，
   避免本地配置触发真实遥测导出及连接重试错误。此设置仅作用于测试命令，
   不修改 `.env` 或生产可观测性，也未增加警告过滤规则。

本次开始时已有的互换业务实现、验收输入和 `response_contains` 改动保留。

## 此前验证命令

使用仓库 `.venv`：Python 3.11.15、pytest 9.0.3。

```powershell
$env:PYTHONUTF8 = '1'
$env:ENABLE_LANGFUSE = 'false'
.venv/Scripts/python.exe -m pytest tests/ -v -W error -rs
.venv/Scripts/python.exe -m ruff check app/ tests/
.venv/Scripts/python.exe -m ruff check scripts/check_fixture_consistency.py scripts/merge_golden.py
.venv/Scripts/python.exe scripts/check_fixture_consistency.py --verbose
.venv/Scripts/python.exe scripts/check_adr_refs.py
.venv/Scripts/python.exe scripts/check_alert_threshold_consistency.py
```

fixture 一致性通过：6 份历史／归档基准，共 1938 条记录；新增节点回归集由 pytest
单独消费。ADR 路径与互引检查、告警阈值一致性及 `git diff --check` 均通过。

测试范围沿用仓库配置：`tests/api` 需要真实后端和 VPN，已由 `pyproject.toml`
原有配置排除。15 项条件跳过分别为 ticker fixture 中 14 条没有 `winner` 的格式检查，
以及 Windows 不支持 chmod 的 1 项测试；未新增 skip、xfail 或降低断言标准。

## Golden 准确率对比待办

清单原命令 `python scripts/eval_golden.py` 缺少必填的 `golden_path`。
补全实际路径后运行：

```powershell
.venv/Scripts/python.exe scripts/eval_golden.py tests/fixtures/old_typing/golden.jsonl --parallel 1
```

脚本在构造请求时因 `KeyError: 'raw_content'` 退出，尚未发出评估请求。
当前 535 条记录全部使用 `conversation` 格式，旧脚本要求顶层 `raw_content`；
其默认端点也仍是旧 `/v1/message`。因此本次没有有效准确率结果，不能宣称不低于上一版。

需要使用支持当前多轮数据和接口的评估入口，取得可用的评估服务端点以及同一数据集、
同一配置的上一版报告，再做准确率对比。已向用户请求端点和基线信息。

## 中文 PR 变更说明

建议标题：`feat(swap): 补齐全新下单交易对手识别与回归验证`

全新文本下单在提交前增加独立交易对手召回节点，按候选归属、原文证据、唯一性及已有
订单冲突规则校验后补全名称。补齐节点及图路由测试、6 条 golden 工程回归样例，
并修复基准文件迁移后遗留的测试、脚本和文档引用。

本地验证及适用范围见上文；真实模型 Golden 准确率对比仍待完成。
