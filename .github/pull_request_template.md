## 概述

<!-- 一句话说明本 PR 做了什么、为什么 -->

## 变更内容

-

## 子图归属判定（新增业务场景必填，ADR 0007）

- [ ] 本 PR 不新增业务场景（跳过本节）
- [ ] 已按四条触发规则判定（后端 API 重叠 <30% / 独立前置查询 / 业务方独立产品线 / 预计意图 ≥4）：命中任一 → 独立子图，否则归现有子图新意图，判定结论写在概述里

## 边界判定（Code vs Prompt，改动 LLM 节点 / 提示词必填；rule0916 §2.1）

- [ ] 本 PR 不涉及 LLM 节点 / 提示词（跳过本节）
- [ ] 三问均为"否"才归 LLM，结论写进概述；归 LLM 的节点已写明为何不能归 Code：
  1. 能否用正则 / 字典 / 枚举映射 / 规则表 100% 覆盖？（能 → Code）
  2. 输出是否为客户可见文本（回复 / 追问 / 确认卡片）？（是 → Code 模板）
  3. 是否涉及金额 / 数量 / 日期 / 方向 / 标的代码等交易关键字段的最终取值？（是 → Code，后端权威校验）

## 提交检查清单

- [ ] `pytest tests/ -v` 全部通过（既有环境性失败除外，需注明）
- [ ] `ruff check app/ tests/` 零新增警告
- [ ] 改了提示词：字符数前后对比（只减不增）已写明；已跑 golden 回归且通过率不低于上一版（`prompt(<scope>)` commit 类型）
- [ ] 新增节点/意图：golden set 已加 case（每意图 ≥2 条），路由表 `_INTENT_TO_NODE` 与 path_map 两处都改了
- [ ] 改了 State：`business_params.py` 校验与 `make_initial_state()` 同步
- [ ] 改了告警阈值：按 ADR 0019 §5 五步同步
- [ ] 无硬编码 secret / 业务数据字典（CLAUDE.md 红线）

## 关联

<!-- Closes #xx / ADR 编号 -->
